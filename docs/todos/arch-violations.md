# Architecture Audit Report

**Date**: 2026-02-24 (19th audit)
**Branch**: main (commit 1c1e9b928)
**Status**: DEAD CODE — removed dead `ref_symbol()` and legacy Ref(SymbolRef) code paths (~275 LOC)

---

## Audit Scope

Checked all architecture rules from CLAUDE.md and NORTH_STAR.md:

1. TypeKey/TypeData leakage outside solver crate
2. Solver imports in binder
3. Checker files exceeding 2000 LOC
4. Forbidden cross-layer imports (emitter->checker, binder->solver, checker->solver internals, CLI->checker internals)
5. Checker pattern-matching on low-level type internals
6. TS2322 routing compliance

---

## Findings

### 1. TypeKey/TypeData Leakage Outside Solver — CLEAN

No `TypeKey` or `TypeData` type imports found outside the solver crate. All references to "TypeKey" in scanner/parser are `SyntaxKind::TypeKeyword` (the `type` keyword token), which is unrelated.

- No `TypeData` imports in checker, binder, emitter, LSP, or CLI code
- No direct pattern matching on `TypeData` variants outside solver
- Checker uses only public solver API: `TypeId`, `DefId`, `TypeFormatter`, `QueryDatabase`, `TypeEnvironment`, `Judge`, etc.
- 25 `TypeData` mentions in checker are all in comments/documentation (architectural notes), not actual code usage

### 2. Solver Imports in Binder — CLEAN

The binder crate (`tsz-binder`) depends only on `tsz-common`, `tsz-scanner`, and `tsz-parser`. Zero imports of solver or checker types found. No `TypeId`, `TypeData`, `TypeInterner`, or solver module references in any binder source file.

### 3. Checker File Sizes — COMPLIANT (5 near-threshold files)

All checker files are under the 2000-line limit. Many previously near-threshold files have been successfully split. Current files to monitor:

| File | Lines | Headroom |
|------|-------|----------|
| `state_checking_members/ambient_signature_checks.rs` | 1,301 | 699 lines |
| `types/type_node.rs` | 1,534 | 466 lines |
| `types/property_access_type.rs` | 1,569 | 431 lines |
| `state/type_analysis/mod.rs` | 1,557 | 443 lines |
| `types/type_checking/duplicate_identifiers.rs` | 1,549 | 451 lines |
| `state/type_resolution/core.rs` | 1,542 | 458 lines |
| `types/type_checking/core.rs` | 1,538 | 462 lines |
| ~~`declarations/import_declaration_checker.rs`~~ | ~~1,620~~ | ~~380 lines~~ | ✅ Split — extracted import-equals validation (~672 LOC) into `import_equals_checker.rs`, reducing to 958 LOC |
| ~~`state/state_property_checking.rs`~~ | ~~1,611~~ | ~~389 lines~~ | ✅ Split (81939ca00) — extracted readonly assignment checking (~510 LOC) into `state_readonly_checking.rs`, reducing to 1,099 LOC |
| ~~`flow/control_flow.rs`~~ | ~~1,734~~ | ~~266 lines~~ | ✅ Split — extracted definite assignment traversal + variable declaration utilities (~370 LOC) into `control_flow_var_utils.rs`, reducing to 1,364 LOC |
| ~~`types/utilities/jsdoc.rs`~~ | ~~1,675~~ | ~~325 lines~~ | ✅ Split (46d0ff829) — extracted param tag validation, comment finding, text parsing (~740 LOC) into `jsdoc_params.rs`, reducing to 934 LOC |
| ~~`checkers/member_declaration_checks.rs`~~ | ~~1,728~~ | ~~272 lines~~ | ✅ Split — extracted class type param ref checks, abstract overload/consecutive checks (~462 LOC) into `class_type_param_checks.rs`, reducing to 1,266 LOC |
| ~~`state_checking_members/member_access.rs`~~ | ~~1,681~~ | ~~319 lines~~ | ✅ Split (94751b2c3) — extracted index signature checks (~575 LOC) into `index_signature_checks.rs`, reducing to 1,108 LOC |
| ~~`flow/flow_graph_builder.rs`~~ | ~~1,666~~ | ~~334 lines~~ | ✅ Split — extracted expression flow handling (~526 LOC) into `flow_graph_builder_expressions.rs`, reducing to 1,171 LOC |
| ~~`types/type_checking_queries_class.rs`~~ | ~~1,713~~ | ~~287 lines~~ | ✅ Split (9de7747a3) — extracted lib type resolution (~686 LOC) into `queries/lib_resolution.rs`, reducing to 1,070 LOC |
| ~~`types/type_checking_global.rs`~~ | ~~1,897~~ | ~~103 lines~~ | ✅ Split (64a0a757b) — extracted duplicate identifier/declaration conflict checking (~1,544 LOC) into `type_checking_duplicate_identifiers.rs`, reducing to 363 LOC |

Previously near-threshold files (all successfully split): `state_class_checking.rs` (919), `type_computation_call.rs` (796), `member_declaration_checks.rs` (1,695), `type_computation_access.rs` (889), `type_checking_queries_lib.rs` (1,313), `control_flow_narrowing.rs` (1,204), `type_computation.rs` (1,119), `control_flow_assignment.rs` (878), `class_type.rs` (1,026).

**Note**: The `queries/lib.rs` file (previously `type_checking_queries_lib.rs`) remains stable at ~1,313 lines. The perf commit (b81760973) extracted new logic into a separate `queries/lib_prime.rs` (113 lines). All 7 `type_checking_queries_*` files were reorganized into `types/queries/` subdirectory (c0b736bce).

**Note**: The 7 `flow/control_flow_*.rs` files were reorganized into `flow/control_flow/` subdirectory (a1b53e74f), mirroring the binder `state_*.rs` → `state/` pattern. The 3 `flow_analysis_*.rs` files were similarly reorganized into `flow/flow_analysis/` subdirectory. The 2 `flow_graph_builder*.rs` files were similarly reorganized into `flow/flow_graph_builder/` subdirectory. All flow-domain file groups now use the subdirectory pattern.

**Note**: The 6 `state/state_*_checking.rs` files were reorganized into `state/state_checking/` subdirectory (36bdf4523), following the same subdirectory pattern. Renames: `state_checking.rs` → `mod.rs`, `state_class_checking.rs` → `class.rs`, `state_directive_checking.rs` → `directive.rs`, `state_heritage_checking.rs` → `heritage.rs`, `state_property_checking.rs` → `property.rs`, `state_readonly_checking.rs` → `readonly.rs`.

**Note**: The 2 `types/class_type*.rs` files were reorganized into `types/class_type/` subdirectory (2a3ac1a83). Renames: `class_type.rs` → `class_type/mod.rs`, `class_type_constructor.rs` → `class_type/constructor.rs`.

**Note**: The 3 `declarations/import_*.rs` files were reorganized into `declarations/import/` subdirectory. Renames: `import_checker.rs` → `import/mod.rs`, `import_declaration_checker.rs` → `import/declaration.rs`, `import_equals_checker.rs` → `import/equals.rs`.

**Note**: The 5 `query_boundaries/state_*.rs` files were reorganized into `query_boundaries/state/` subdirectory (e6969996e). Renames: `state.rs` → `state/mod.rs`, `state_checking.rs` → `state/checking.rs`, `state_type_analysis.rs` → `state/type_analysis.rs`, `state_type_environment.rs` → `state/type_environment.rs`, `state_type_resolution.rs` → `state/type_resolution.rs`. ~~Remaining `query_boundaries/` candidates for future subdirectory grouping: `type_computation_*` (3 files).~~ ✅ Reorganized into `query_boundaries/type_computation/` subdirectory. Renames: `type_computation.rs` → `type_computation/core.rs`, `type_computation_access.rs` → `type_computation/access.rs`, `type_computation_complex.rs` → `type_computation/complex.rs`. ~~checker-specific boundaries (6 `*_checker.rs` files)~~ ✅ Reorganized into `query_boundaries/checkers/` subdirectory (c681e5712). Renames: `call_checker.rs` → `checkers/call.rs`, `constructor_checker.rs` → `checkers/constructor.rs`, `generic_checker.rs` → `checkers/generic.rs`, `iterable_checker.rs` → `checkers/iterable.rs`, `promise_checker.rs` → `checkers/promise.rs`, `property_checker.rs` → `checkers/property.rs`. All `query_boundaries/` file groups now use the subdirectory pattern.

**Note**: Three `state/` subdirectory `mod.rs` files had implementation logic extracted into `core.rs` (9e2355b37). `state/type_analysis/mod.rs` (1557 → 7 LOC), `state/state_checking/mod.rs` (1349 → 10 LOC), `state/type_environment/mod.rs` (1326 → 5 LOC).

**Note**: Three `flow/` subdirectory `mod.rs` files had implementation logic extracted into `core.rs`. `flow/control_flow/mod.rs` (1389 → 30 LOC), `flow/flow_analysis/mod.rs` (1261 → 32 LOC), `flow/flow_graph_builder/mod.rs` (1128 → 16 LOC).

**Note**: `declarations/import/mod.rs` had implementation logic extracted into `core.rs` (a77614639). `declarations/import/mod.rs` (1294 → 5 LOC).

**Note**: `context/mod.rs` had implementation logic extracted into `core.rs`. `context/mod.rs` (1093 → 642 LOC). Moved `impl TypeCache`, `impl CheckerContext`, and TypeCache tests into `context/core.rs` (469 LOC). Remaining mod.rs files with heavy implementation: `types/class_type/mod.rs` (1013 LOC), ~~`types/utilities/mod.rs` (1007 LOC)~~ ✅ Extracted into `types/utilities/core.rs` (1007 LOC), reducing mod.rs to 7 LOC.

**Note**: `types/utilities/mod.rs` had implementation logic extracted into `core.rs`. `types/utilities/mod.rs` (1007 → 7 LOC). ~~Remaining mod.rs files with heavy implementation: `types/class_type/mod.rs` (1013 LOC).~~ ✅ All mod.rs files now use thin re-export pattern.

**Note**: `types/class_type/mod.rs` had implementation logic extracted into `core.rs`. `types/class_type/mod.rs` (1013 → 6 LOC). All checker subdirectory `mod.rs` files now follow the thin-mod.rs pattern with implementation in `core.rs`.

**Note — Solver thin-mod.rs migration**: `narrowing/mod.rs` had implementation logic extracted into `core.rs`. `narrowing/mod.rs` (1450 → 54 LOC). Three private methods (`narrow_type_param`, `is_js_primitive`, `is_assignable_to`) promoted to `pub(super)` for sibling module access. `intern/mod.rs` had implementation logic extracted into `core.rs`. `intern/mod.rs` (1416 → ~50 LOC). `intern_callable_shape` promoted to `pub(super)` for sibling access. `operations/mod.rs` had implementation logic extracted into `core.rs`. `operations/mod.rs` (1106 → ~75 LOC). `force_bivariant_callbacks` field promoted to `pub(super)` for sibling access. `diagnostics/mod.rs` had implementation logic extracted into `core.rs`. `diagnostics/mod.rs` (924 → 19 LOC). ~~`type_queries/mod.rs`~~ ✅ had implementation logic extracted into `core.rs`. `type_queries/mod.rs` (894 → 87 LOC). ~~`contextual/mod.rs`~~ ✅ had implementation logic extracted into `core.rs`. `contextual/mod.rs` (799 → 18 LOC). All solver subdirectory `mod.rs` files now follow the thin-mod.rs pattern.

**Note — Solver subtype/ subdirectory migration** (f49dc5cca): 5 `subtype_*.rs` files + `subtype_rules/` reorganized into `relations/subtype/` subdirectory. Renames: `subtype.rs` → `subtype/core.rs`, `subtype_cache.rs` → `subtype/cache.rs`, `subtype_explain.rs` → `subtype/explain.rs`, `subtype_helpers.rs` → `subtype/helpers.rs`, `subtype_overlap.rs` → `subtype/overlap.rs`, `subtype_visitor.rs` → `subtype/visitor.rs`, `subtype_rules/` → `subtype/rules/`. All external paths stable via re-exports. Unused `SubtypeFailureReason`/`SubtypeChecker`/`SubtypeResult`/`TypeResolver` convenience re-exports in `relations/mod.rs` removed (no consumers). ~~Remaining `relations/` candidates for subdirectory grouping: `compat*.rs` (2 files) — not yet warranted.~~

**Note — Module style normalization** (9c7b34339): Converted 4 library modules from `foo.rs` + `foo/` pattern to `mod.rs` pattern (73 existing modules use `mod.rs`). Affected: `checker/error_reporter`, `common/diagnostics`, `lsp/code_actions`, `lsp/completions`. One remaining outlier: `tsz-cli/src/bin/tsz.rs` + `tsz/` — this is a binary entry point, not a library module, so the alternate pattern is appropriate.

**Note — Emitter thin-mod.rs migration**: `declaration_emitter/mod.rs` had implementation logic extracted into `core.rs`. `declaration_emitter/mod.rs` (1484 → 42 LOC). Five methods promoted to `pub(super)` for sibling module access: `collect_imported_symbols_from_clause`, `emit_statement`, `emit_class_member`, `emit_parameter_properties`, `is_symbol_call`. ~~`emitter/mod.rs` (1420 LOC)~~ ✅ extracted into `emitter/core.rs` (1360 LOC), reducing mod.rs to 85 LOC. Six methods promoted to `pub(crate)` for sibling access: `source_text_for_map`, `emit_node_by_kind`, `is_single_line`, `are_on_same_line_in_source`, `emit_object_property`, `estimate_output_capacity`. Helper structs (`ParamTransformPlan`, `ParamTransform`, `RestParamTransform`, `TempScopeState`, `TemplateParts`) promoted to `pub(crate)`. ~~Remaining emitter `mod.rs` file with heavy implementation: `lowering/mod.rs` (1383 LOC).~~ ✅ extracted into `lowering/core.rs` (1338 LOC), reducing mod.rs to 72 LOC. `visit` method and all struct fields promoted to `pub(super)` for sibling module access. All emitter subdirectory `mod.rs` files now follow the thin-mod.rs pattern.

**Note — Lowering crate thin-mod.rs migration** (764ef7f61): `tsz-lowering/src/lower/mod.rs` had implementation logic extracted into `core.rs`. `lower/mod.rs` (1710 → 9 LOC). Twelve private methods promoted to `pub(super)` for sibling module access from `advanced.rs`: `resolve_def_id_by_name`, `resolve_type_symbol`, `resolve_def_id`, `resolve_value_symbol`, `push_type_param_scope`, `pop_type_param_scope`, `add_type_param_binding`, `lookup_type_param`, `with_type_params`, `collect_type_parameters`, `lower_type_parameter`, `lower_params_with_this`, `lower_return_type`, `collect_interface_members`. All struct fields and internal types (`InterfaceParts`, `PropertyMerge`, `MethodOverloads`, type aliases) promoted to `pub(super)`. ~~Remaining non-checker/solver/emitter `mod.rs` files with heavy implementation: `tsz-lsp/src/project/mod.rs` (1691 LOC)~~ ✅ extracted into `project/core.rs` (1681 LOC), reducing mod.rs to 17 LOC. No visibility promotions needed — all sibling-referenced types were already `pub(crate)` or `pub`. ~~Remaining non-checker/solver/emitter `mod.rs` files with heavy implementation: `tsz-binder/src/state/mod.rs` (1470 LOC),~~ ✅ extracted into `state/core.rs` (1111 LOC), reducing mod.rs to 379 LOC. No visibility promotions needed — `BinderStateScopeInputs::with_scopes` promoted to `pub(super)`. ~~`tsz-lsp/src/hover/mod.rs` (1168 LOC)~~ ✅ extracted into `hover/core.rs` (1128 LOC), reducing mod.rs to 50 LOC. No visibility promotions needed. ~~`tsz-lsp/src/rename/mod.rs` (960 LOC)~~ ✅ extracted into `rename/core.rs` (714 LOC), reducing mod.rs to 258 LOC. `rename_target_node` promoted to `pub(super)`, free helpers `extract_identifier_from_source` and `is_non_renamable_builtin` promoted to `pub(super)`. ~~`completions/mod.rs` (870 LOC)~~ ✅ extracted into `completions/core.rs` (562 LOC), reducing mod.rs to 325 LOC. Two methods promoted to `pub(super)` for sibling module access: `determine_completion_kind`, `member_completion_target`. All `Completions` struct fields promoted to `pub(super)`. ~~Remaining LSP `mod.rs` files with heavy implementation: `resolver/mod.rs` (812 LOC).~~ ✅ extracted into `resolver/core.rs` (798 LOC), reducing mod.rs to 14 LOC. No visibility promotions needed — `ScopeWalker` fields were already `pub(crate)` or private, and `children.rs` accesses `ScopeWalker` via `super::` re-export. All LSP subdirectory `mod.rs` files now follow the thin-mod.rs pattern.

**Note — CLI driver/ subdirectory migration** (e2fcf4414): 9 `driver_*.rs` files reorganized into `driver/` subdirectory. Renames: `driver.rs` → `driver/core.rs`, `driver_check.rs` → `driver/check.rs`, `driver_check_utils.rs` → `driver/check_utils.rs`, `driver_emit.rs` → `driver/emit.rs`, `driver_resolution.rs` → `driver/resolution.rs`, `driver_sources.rs` → `driver/sources.rs`, plus their test files. `driver_emit` and `driver_resolution` moved from separate top-level `lib.rs` modules to `driver::emit` and `driver::resolution` submodules. All external paths stable via re-exports in `driver/mod.rs`. `lib.rs` reduced from 3 driver modules to 1.

**Note — Emitter module_emission/ subdirectory migration** (50048603c): 3 `module_emission_*.rs` files reorganized into `module_emission/` subdirectory. Renames: `module_emission.rs` → `module_emission/core.rs`, `module_emission_exports.rs` → `module_emission/exports.rs`, `module_emission_imports.rs` → `module_emission/imports.rs`. All `pub(super)` methods promoted to `pub(in crate::emitter)` for sibling module access (matching `es5/` pattern). ~~Remaining emitter `prefix_suffix.rs` candidates for subdirectory grouping: `expressions_*` (5 files, 4333 LOC), `declarations_*` (4 files, 4251 LOC).~~

**Note — Emitter declarations/ subdirectory migration** (dc68980af): 4 `declarations_*.rs` files reorganized into `declarations/` subdirectory. Renames: `declarations.rs` → `declarations/core.rs`, `declarations_class.rs` → `declarations/class.rs`, `declarations_class_members.rs` → `declarations/class_members.rs`, `declarations_namespace.rs` → `declarations/namespace.rs`. All `pub(super)` methods promoted to `pub(in crate::emitter)` for sibling module access. ~~Remaining emitter candidate for subdirectory grouping: `expressions_*` (5 files, ~4333 LOC).~~

**Note — Emitter expressions/ subdirectory migration**: 5 `expressions_*.rs` files reorganized into `expressions/` subdirectory. Renames: `expressions.rs` → `expressions/core.rs`, `expressions_access.rs` → `expressions/access.rs`, `expressions_binary_downlevel.rs` → `expressions/binary_downlevel.rs`, `expressions_call.rs` → `expressions/call.rs`, `expressions_literals.rs` → `expressions/literals.rs`. All `pub(super)` methods promoted to `pub(in crate::emitter)` for sibling module access. All emitter prefix-grouped file families now use the subdirectory pattern.

**Note — Emitter comments/ subdirectory migration** (5dcdc1506): 2 comment-related files reorganized into `comments/` subdirectory. Renames: `comments.rs` → `comments/core.rs`, `comment_helpers.rs` → `comments/helpers.rs`. All `pub(super)` methods promoted to `pub(in crate::emitter)` for sibling module access. ~~Remaining emitter top-level file candidates for future grouping: `literals.rs` (843 LOC) + `template_literals.rs` (320 LOC)~~, `type_printer.rs` (654 LOC) + `types.rs` (271 LOC).

**Note — Emitter literals/ subdirectory migration** (0460b15f5): 2 literal-related files reorganized into `literals/` subdirectory. Renames: `literals.rs` → `literals/core.rs`, `template_literals.rs` → `literals/template.rs`. All `pub(super)` methods promoted to `pub(in crate::emitter)` for sibling module access. Remaining emitter top-level file candidates for future grouping: `type_printer.rs` (654 LOC) + `types.rs` (271 LOC).

**Note — Long functions to address**: `find_circular_reference_in_type_node` (1110 lines, `state/variable_checking/core.rs:15`), `in_progress_class_instance_result` (951 lines, `types/class_type/core.rs:35`), `should_cache_base_expr_result` (847 lines, `state/type_resolution/constructors.rs:15`), `get_type_of_property_access_inner` (809 lines, `types/property_access_type.rs:25`), `dispatch_type_computation` (754 lines, `dispatch.rs:478`).

### 4. Cross-Layer Imports — CLEAN

- **Emitter -> Checker**: No `tsz_checker` imports in emitter. Emitter depends on parser, binder, solver only.
- **Binder -> Solver**: No solver dependencies in binder (see finding #2).
- **Checker -> Solver internals**: No raw `TypeData::` constructions or direct `intern()` calls in checker code. Checker uses public solver API constructors and query boundary helpers.
- **CLI -> Checker internals**: CLI and LSP crates import only public checker exports.
- **Solver -> Parser/Checker**: No upward imports. Solver is a pure type system layer.
- **Lowering -> Checker**: No checker dependency. Lowering bridges AST and Solver only.

**Note on TypeInterner usage**: LSP, CLI, and Emitter import `TypeInterner` from `tsz-solver`. This is **expected architecture** — `TypeInterner` is the public read-only type store. What's forbidden is importing `TypeData` variants or performing direct type construction, not read-only type store access.

**Note on Solver -> Binder (`SymbolId`)**: The solver crate depends on `tsz-binder` for the `SymbolId` identity handle. This is **by design** — `SymbolId(u32)` is a shared identity handle (CLAUDE.md §7) required for type variants like `TypeQuery(SymbolRef)` and `UniqueSymbol(SymbolRef)`. The forbidden pattern is binder importing solver for *semantic decisions*, not shared identity handles flowing between layers.

### 5. Checker Type Internals Pattern-Matching — CLEAN

- All type shape inspection delegated to solver queries via query boundaries
- Type traversal properly routed through `tsz_solver::visitor::` helpers (`collect_lazy_def_ids`, `collect_type_queries`, `collect_referenced_types`, `collect_enum_def_ids`, `is_template_literal_type`)
- Architecture contract tests in `architecture_contract_tests.rs` actively prevent direct `TypeData::Array`, `TypeData::ReadonlyType`, `TypeData::KeyOf`, `TypeData::IndexAccess`, `TypeData::Lazy(DefId)`, and `TypeData::TypeParameter` construction in checker

### 6. TS2322 Routing — COMPLIANT

- `CompatChecker` is only instantiated from `query_boundaries/call_checker.rs` and `query_boundaries/assignability.rs`
- No direct `CompatChecker` calls in checker feature modules
- Assignability checks route through the centralized gateway
- 26 query_boundaries modules properly gate all solver access

### 7. Previously Fixed: TypeData Traversal in tsz-lowering — REMAINS FIXED

The `collect_infer_bindings` method was moved from `tsz-lowering` into `tsz-solver/src/visitors/visitor_extract.rs` in commit f5aa685e7. The lowering crate now calls the solver-owned utility. No regression.

---

## CI Health

CI was red for ~15 runs due to emit JS baseline mismatch. Commit 117acf1a4 manually set README baseline to 67.9% (9,254) based on local results, but CI consistently measured 67.8% (9,242-9,243). Fixed by updating README baseline to 67.8% (9,243) and DTS to 35.1% (762) in commit b9db67920.

**Lesson**: Always let CI/automated scripts set baselines. Manual baseline overrides from local results can diverge from CI environment behavior (timeout sensitivity, parallelism differences).

---

## Recommendations

1. **Monitor near-limit checker files** for growth. Top files by line count:
   - ~~`type_checking_queries_lib.rs` (1,901 lines)~~ ✅ Split — extracted type-only detection methods (~590 LOC) into `type_checking_queries_type_only.rs`, reducing to 1,313 LOC
   - ~~`control_flow_narrowing.rs` (1,883 lines)~~ ✅ Split — extracted reference matching, literal parsing, and symbol resolution (~680 LOC) into `control_flow_references.rs`, reducing to 1,204 LOC
   - ~~`control_flow_assignment.rs` (1,837 lines)~~ ✅ Split — extracted condition-based narrowing (switch, binary, logical, typeof/instanceof) (~970 LOC) into `control_flow_condition_narrowing.rs`, reducing to 878 LOC
   - ~~`class_type.rs` (1,818 lines)~~ ✅ Split — extracted constructor type resolution (~811 LOC) into `class_type_constructor.rs`, reducing to 1,025 LOC
   - ~~`type_checking_utilities.rs` (1,778 lines)~~ ✅ Split — extracted return type inference (~776 LOC) into `type_checking_utilities_return.rs`, reducing to 1,002 LOC
   - ~~`assignability_checker.rs` (1,447 lines)~~ ✅ Split — extracted subtype/identity/compat methods (~273 LOC) into `subtype_identity_checker.rs`, reducing to 1,176 LOC
2. **Monitor near-threshold files** in the 1,700-1,900 range for growth.
3. **Solver top-level file sprawl**: Remaining file families to organize into subdirectories (following the pattern now established by `narrowing/`, `relations/`, `evaluation/`, `inference/`, `instantiation/`, `visitors/`, `caches/`, `operations/`):
   - ~~`operations_*.rs` (10 files, 7,863 LOC) → `operations/` subdirectory~~ ✅ Done (c3365ed0d)
   - ~~`binary_ops.rs` (970 LOC) → `operations/binary_ops.rs`~~ ✅ Done (1a0a1f886) — was at top level but logically part of operations; `operations/mod.rs` was already re-exporting from it.
   - ~~`type_queries_*.rs` (5 files) → `type_queries/` subdirectory~~ ✅ Done
   - ~~`intern_*.rs` (4 files: `intern.rs`, `intern_normalize.rs`, `intern_intersection.rs`, `intern_template.rs`) → `intern/` subdirectory~~ ✅ Done
   - ~~Re-export shim files (7 files: `compat.rs`, `db.rs`, `evaluate.rs`, `infer.rs`, `instantiate.rs`, `query_trace.rs`, `subtype.rs`) removed~~ ✅ Done (a727b3b8b) — internal imports updated to direct module paths. Two externally-used shims (`judge.rs`, `visitor.rs`) retained.
   - ~~`contextual.rs` (1,693 LOC)~~ ✅ Split and grouped — first extracted 8 TypeVisitor implementations (~909 LOC) into `contextual_extractors.rs`, then moved both files into `contextual/` subdirectory (`contextual.rs` → `contextual/mod.rs`, `contextual_extractors.rs` → `contextual/extractors.rs`). The extractors are `pub(crate)` implementation details used only by `ContextualTypeContext`.
   - ~~`freshness.rs` (37 LOC) + `variance.rs` (558 LOC) → `relations/` subdirectory~~ ✅ Done (fd881b926) — both are relation-adjacent concepts (excess property freshness, type parameter variance for assignability).
   - ~~`apparent.rs`/`objects.rs`/`object_literal.rs`/`index_signatures.rs`/`element_access.rs` (object/property-adjacent, ~1,450 LOC) → `objects/` subdirectory~~ ✅ Done (943fe7f50) — apparent→objects/apparent, objects→objects/collect, object_literal→objects/literal, index_signatures and element_access kept names.
   - ~~`class_hierarchy.rs`/`inheritance.rs` (class-adjacent, ~511 LOC total) → `classes/` subdirectory~~ ✅ Done — class type construction and nominal inheritance graph grouped together.
   - ~~`diagnostics.rs` (1,690 LOC)~~ ✅ Split — extracted eagerly-rendered builders (`DiagnosticBuilder`, `SpannedDiagnosticBuilder`, `SourceLocation`, `DiagnosticCollector`, ~664 LOC) into `diagnostics_builders.rs`, reducing `diagnostics.rs` to ~1,002 LOC. Core file now contains tracer pattern, failure reasons, lazy diagnostics, codes, and PendingDiagnosticBuilder.
   - ~~`diagnostics.rs` (1,002 LOC) + `diagnostics_builders.rs` (664 LOC) → `diagnostics/` subdirectory~~ ✅ Done (dc40a5cfd) — `diagnostics.rs` → `diagnostics/mod.rs`, `diagnostics_builders.rs` → `diagnostics/builders.rs`.
   - ~~`expression_ops.rs` (332 LOC) + `compound_assignment.rs` (75 LOC) → `operations/` subdirectory~~ ✅ Done (5d422ee20) — both are pure type operations; joined `binary_ops` in existing `operations/` directory.
   - ~~`widening.rs` (128 LOC) → `operations/widening.rs`~~ ✅ Done (f306f0f4b)
   - ~~`application.rs` (190 LOC) → `instantiation/application.rs`~~ ✅ Done (db9084e91) — type application evaluation is conceptually part of the instantiation pipeline.
   - ~~`type_factory.rs` (184 LOC) → `intern/type_factory.rs`~~ ✅ Done (c492109bf) — type construction facade is conceptually adjacent to the interning engine.
   - ~~`format.rs` (787 LOC) → `diagnostics/format.rs`~~ ✅ Done — type formatting is the primary consumer of diagnostics infrastructure and its sole internal caller is `diagnostics/builders.rs`.
   - ~~`def.rs` (581 LOC) + `type_resolver.rs` (593 LOC) → `def/` subdirectory~~ ✅ Done (795d697bd) — `def.rs` → `def/core.rs` (DefId, DefinitionStore, DefKind, DefInfo), `type_resolver.rs` → `def/resolver.rs` (TypeResolver trait, TypeEnvironment). Both form the DefId-first semantic type resolution system (§6).
   - Remaining top-level candidates: `unsoundness_audit.rs` (830 LOC — not runtime code, could move to docs); `recursion.rs` (664 LOC) + `canonicalize.rs` (609 LOC) could group together.
4. ~~**Checker `context*.rs` files**: organized into `context/` subdirectory~~ ✅ Done
5. ~~**Solver `type_queries/extended.rs`** (1,915 LOC): approaching 2000-line limit~~ ✅ Done — extracted constructor/class/instance classifiers (~482 LOC) into `extended_constructors.rs`, reducing `extended.rs` to ~1,442 LOC.
6. **Solver `type_queries/mod.rs`** reduced from 1,947 → 1,744 → 1,395 LOC by extracting iterable classifications into `iterable.rs` and then traversal/property-access classifications (~355 LOC) into `traversal.rs`. Remaining sections: core type queries, intrinsic queries, composite queries, constructor/static collection, construct signatures, constraint classification, signature classification, property lookup classification, evaluation-needed classification.
7. **Solver `visitors/visitor.rs`** reduced from 1,945 → ~1,130 LOC by extracting type predicates (`is_*`, `contains_*`, `classify_*`, `ObjectTypeKind`) and their internal helper structs into `visitor_predicates.rs` (~585 LOC). The `ConstAssertionVisitor` (~178 LOC) remains in `visitor.rs` but could be extracted if the file grows again.
8. **Solver `relations/subtype.rs`** reduced from 1,899 → 1,568 LOC by extracting the caching/cycle-detection layer (`check_subtype` method, ~331 LOC) into `subtype_cache.rs`. The main file now focuses on structural dispatch (`check_subtype_inner`) while the cache file handles fast paths, memoization, coinductive cycle detection (TypeId, DefId, SymbolId levels), and pre-evaluation intrinsic checks.
9. **Binder `state_binding.rs`** reduced from 1,992 → 1,679 LOC by extracting post-binding validation, lib symbol diagnostics, and resolution statistics (~313 LOC) into `state_binding_validation.rs`.
10. **Binder `state_node_binding.rs`** reduced from 1,950 → 1,397 LOC by extracting name collection utilities and modifier helpers (~553 LOC) into `state_node_binding_names.rs`. The extracted file consolidates six identical `has_*_modifier` functions into a shared `has_modifier` helper.
   - ~~**Checker `ambient_signature_checks.rs`** (1,767 LOC)~~ ✅ Split (b0cf24ed4) — extracted overload compatibility, implicit-any return checks, modifier combinations, and signature utilities (~466 LOC) into `overload_compatibility.rs`, reducing to 1,301 LOC.
   - ~~**Checker `type_node.rs`** (1,765 LOC)~~ ✅ Deduplicated (9ca67eac4) — extracted 4 identical type/value/def_id resolver closure blocks into shared helper methods (`resolve_type_symbol`, `resolve_value_symbol`, `resolve_value_symbol_with_libs`, `resolve_def_id`, `resolve_def_id_with_qualified_names`, `lower_with_resolvers`), reducing to 1,522 LOC.
11. ~~**Solver `caches/db.rs`** (1,896 LOC): approaching 2000-line limit~~ ✅ Split — extracted `QueryCache` struct and all its impl blocks (~1,006 LOC) into `caches/query_cache.rs`, reducing `db.rs` to ~882 LOC. The trait file now contains only `TypeDatabase`/`QueryDatabase` trait definitions and their `TypeInterner` implementations; the cache file contains the concrete `QueryCache` implementation.
12. ~~**DRY violation: `emit_exported_variable` vs `emit_variable_declaration_statement`**~~ ✅ Done (8eb4e84af) — extracted shared `emit_variable_decl_type_or_initializer` helper in `helpers.rs`. Also fixed latent bug where export path incorrectly included `NullKeyword` in literal initializer check (`const x = null` emitted `= null` instead of `: any`).
13. ~~**Emit JS pass rate regression**: Commit 92ba86966 ("Fix auto-accessor emit ordering") caused JS emit pass rate to drop from 67.9% → 67.7% (23 fewer passing tests).~~ ✅ Fixed — root cause was unconditional `mark_class_helpers` call for auto-accessor classes at ALL targets. The `__classPrivateFieldGet`/`Set` helpers were emitted even for ES2022+/ESNext where native syntax should be used. Fix gates both helper marking (`lowering_pass.rs`) and WeakMap emission (`declarations.rs`) behind `needs_es2022_lowering`. Pass rate restored to 67.9% (9,254 tests, +8 net improvement).
14. ~~**Misplaced non-source files in checker `src/`**: `keyof-type-checking/` (scaffolded skill template with placeholders) and `state_orchestration_docs.md` (orphaned architectural doc) were sitting inside `crates/tsz-checker/src/`.~~ ✅ Removed (c0cc2fb4c).
15. ~~**Checker top-level file sprawl**: assignability/assignment/subtype_identity checkers~~ ✅ Done (d45a73578) — moved `assignability_checker.rs`, `assignment_checker.rs`, `subtype_identity_checker.rs` into `assignability/` subdirectory (24 → 21 loose files). ~~Remaining grouping candidates: call/param/signature checkers, type-related checkers (generic, iterable, promise).~~ ✅ Done (e38a95d3f) — moved 10 `*_checker.rs` + `signature_builder.rs` files (3,913 LOC) into `checkers/` subdirectory (21 → 11 loose files). ~~Remaining grouping candidates: `error_handler.rs`/`error_reporter.rs` into an `errors/` directory~~ ✅ Done — `error_handler.rs` (668 LOC) removed entirely: the `ErrorHandler` trait was dead abstraction (20+ unused trait methods, unused `DiagnosticBuilder`, unused `&mut CheckerState` blanket impl). Only `emit_error_at` was used (2 call sites); promoted to inherent method on `CheckerState`. Remaining grouping candidates: `decorators.rs`/`optional_chain.rs`/`triple_slash_validator.rs` if more related files accumulate.
15b. ~~**Checker `state_checking_members/` misplaced at src root**: directory was a sibling of `state/` but wired as its submodule via `#[path]` redirects in a proxy file~~ ✅ Done (84646c6f5) — moved 7 files into `state/state_checking_members/` with proper `mod.rs`, eliminating all `#[path]` attributes. Updated architecture test file paths in 3 test files.
16. **Solver re-export shims**: `judge.rs` and `visitor.rs` in solver `src/` are 1-line `pub use` shims (`pub use crate::relations::judge::*` / `pub use crate::visitors::visitor::*`). Used from ~20 call sites in checker/emitter. Low priority — they provide stable convenience paths.
17. **Solver `inference/infer.rs`** reduced from 1,878 → 1,106 LOC by extracting the structural type matching algorithm (~770 LOC) into `infer_matching.rs`. The extracted file contains `infer_from_types` and all its type-shape dispatchers (objects, functions, tuples, callables, unions, intersections, applications, template literals, mapped types). The remaining `infer.rs` now focuses on the inference engine core: union-find, variable management, constraint tracking, and cycle detection.
18. **Solver `relations/compat.rs`** reduced from 1,876 → 1,313 LOC by extracting nominal typing overrides (~564 LOC) into `compat_overrides.rs`. The extracted file contains `is_assignable_with_overrides`, `private_brand_assignability_override`, `enum_assignability_override`, `are_types_identical_for_redeclaration`, and related enum/brand helpers. The remaining `compat.rs` now focuses on core compatibility checking (normalization, weak types, excess properties, structural dispatch).
19. **Solver `narrowing/mod.rs`** reduced from 1,839 → 1,421 LOC by extracting instanceof narrowing (~418 LOC) into `narrowing/instanceof.rs`. The extracted file contains `narrow_by_instanceof`, `narrow_by_instance_type`, and `narrow_by_instanceof_false`.
20. **Solver `operations/mod.rs`** reduced from 1,811 → 1,085 LOC by extracting argument checking, parameter analysis, tuple rest handling, placeholder detection, and contextual sensitivity helpers (~726 LOC) into `operations/call_args.rs`. Next solver files to monitor: `diagnostics.rs` (1,690 LOC), `operations/constraints.rs` (1,530 LOC).
21. **Solver `contextual.rs`** reduced from 1,693 → 805 LOC by extracting 8 `TypeVisitor` extractor implementations and `collect_single_or_union` helper (~909 LOC) into `contextual_extractors.rs`. Extractors: `ThisTypeExtractor`, `ReturnTypeExtractor`, `ThisTypeMarkerExtractor`, `ArrayElementExtractor`, `TupleElementExtractor`, `PropertyExtractor`, `ParameterExtractor`, `ParameterForCallExtractor`, `ApplicationArgExtractor`, plus `extract_param_type_at`.
22. ~~**DRY violation: `LiteralValue` → primitive `TypeId` match blocks**~~ ✅ Done (a5d257efa) — five identical 4-arm match blocks (`String→STRING`, `Number→NUMBER`, `Boolean→BOOLEAN`, `BigInt→BIGINT`) across `expression_ops.rs`, `widening.rs`, and `type_queries/extended.rs` consolidated into `LiteralValue::primitive_type_id()` const method on the enum itself.
23. ~~**Dead modules in solver**: `sound.rs` and `flow_analysis.rs` had zero production callers~~ ✅ Done (2fb24cd8e) — gated as `#[cfg(test)]`, removed `pub use` re-exports, and cleaned up all resulting dead code (unused methods, fields, variants, imports).
24. ~~**Dead function**: `get_required_es_version_for_global` in `checker/context/lib_queries.rs`~~ ✅ Done (2fb24cd8e) — removed (zero callers).
25. **Additional near-threshold checker files** not in the watchlist above: `type_checking_queries_class.rs` (1,751 LOC), `type_checking_utilities_jsdoc.rs` (1,728 LOC), `member_declaration_checks.rs` (1,728 LOC), ~~`state_variable_checking.rs` (1,713 LOC)~~ ✅ Split — extracted for-in/for-of loop variable checking (~388 LOC) into `state_variable_checking_for_loop.rs`, reducing to 1,340 LOC. Remaining: `flow_graph_builder.rs` (1,676 LOC), `type_checking_queries.rs` (1,628 LOC), `type_checking_global.rs` (1,627 LOC), `state_type_analysis.rs` (1,626 LOC), `member_access.rs` (1,622 LOC), `symbol_resolver.rs` (1,581 LOC), `context/mod.rs` (1,573 LOC), `property_access_type.rs` (1,572 LOC), `type_checking.rs` (1,543 LOC), `type_node.rs` (1,535 LOC).
26. **Binder organization** — partially addressed (71657178a): extracted data structures from 735-LOC `lib.rs` into `symbols.rs`, `flow.rs`, `scopes.rs`; `lib.rs` reduced to ~35 LOC facade. ~~`modules/` (state_module_binding + state_import_export + module_resolution_debug)~~ ✅ Done — moved into `modules/` subdirectory as `binding.rs`, `import_export.rs`, `resolution_debug.rs`. ~~`nodes/` (state_node_binding + state_node_binding_names)~~ ✅ Done (df959a9e2) — moved into `nodes/` subdirectory as `binding.rs`, `names.rs`. ~~Remaining: group into `binding/` (state_binding + state_binding_validation)~~ ✅ Done (378875807) — moved into `binding/` subdirectory as `declaration.rs`, `validation.rs`. Remaining top-level `state_*` files: `state_flow_helpers.rs` (127 LOC), `state_lib_merge.rs` (336 LOC), `state_resolution.rs` (590 LOC) — could be grouped if more related files accumulate.
27. ~~**DRY violation: `has_modifier` duplicated in 9+ files**~~ ✅ Done (25388f7e3) — added `NodeArena::has_modifier()`, `has_modifier_ref()`, and `find_modifier()` in `tsz-parser/src/parser/node_modifiers.rs`. Replaced 30+ duplicated modifier-scanning functions across 21 files in binder, checker, emitter, and lowering crates. Net -270 lines. Remaining thin wrappers in checker (`type_checking_queries.rs`) and emitter (`emitter/helpers.rs`, `lowering_pass_helpers.rs`) provide named convenience APIs over the shared utility.
28. ~~**DRY violation: `is_function_like` reimplemented in 4+ locations**~~ ✅ Partially done (f3c3fa1aa) — replaced 4 inline 7-arm kind chains in `flow_analysis_usage.rs` and 1 private method in `call_hierarchy.rs` with `Node::is_function_like()`. Remaining: `tsz-wasm/src/wasm_api/ast.rs:504` (WASM bridge takes raw `u16` kind, not a `Node`; needs a `is_function_like_kind(u16)` free function in parser to fully deduplicate).
29. ~~**DRY violation: LSP hover/definition share 3 identical methods**~~ ✅ Done (47bad28ce) — `find_symbol_query_node_at_or_before`, `is_comment_context`, `should_backtrack_to_previous_symbol` extracted as free functions in `tsz-lsp/src/utils.rs`. Both `HoverProvider` and `GoToDefinition` now call the shared utilities.
30. ~~**DRY violation: type arg constraint-checking loop in `generic_checker.rs`**~~ ✅ Done (5f75ccb21) — three identical TS2344 constraint-checking loops in `validate_call_type_arguments`, `validate_new_expression_type_arguments`, and `validate_type_reference_type_arguments` consolidated into shared `validate_type_args_against_params` helper. Reduced file from 535 to 449 LOC.
31. ~~**DRY violation: erase type params to constraints in `subtype_rules/functions.rs`**~~ ✅ Done (aac1a7a90) — four identical "build TypeSubstitution mapping type params to constraints" loops consolidated into `erase_type_params_to_constraints()` helper.
32. ~~**DRY violation: `get_identifier_text` duplicated in 15 emitter files**~~ ✅ Done (af5b55c60) — consolidated 14 identical copies into shared `identifier_text()` and `identifier_text_or_empty()` in `transforms/emit_utils.rs`. Net -71 lines. Remaining: `lowering_pass_helpers.rs:460` (`get_identifier_text_ref`) returns `&str` (borrow optimization, not a clone).
33. ~~**DRY violation: `is_*` type predicates duplicated between `visitors/visitor_predicates.rs` and `type_queries/mod.rs`**~~ ✅ Done (6774390cf) — 13 identical predicates (`is_union_type`, `is_array_type`, `is_tuple_type`, `is_literal_type`, `is_intersection_type`, `is_conditional_type`, `is_mapped_type`, `is_index_access_type`, `is_template_literal_type`, `is_primitive_type`, `is_empty_object_type`, `is_function_type`, `is_object_like_type`) removed from `type_queries/mod.rs` and replaced with `pub use` re-exports from `visitor_predicates`. Functions with different semantics (`is_type_parameter`, `is_type_reference`) kept separate. Net -138 lines.
34. ~~**DRY violation: `export_decl_has_runtime_value` + `export_clause_is_type_only`**~~ ✅ Done (4a69e301b) — extracted as shared free functions in `transforms/emit_utils.rs`. Both `JsEmitter` (module_emission.rs) and `LoweringPass` (lowering_pass_helpers.rs) now delegate to the shared implementation. Net -65 lines.
35. ~~**DRY violation: `is_instantiated_module`**~~ ✅ Done — extracted `is_instantiated_module` and `is_type_only_module_statement` as shared free functions in `transforms/emit_utils.rs`. Both `Printer` (helpers.rs) and CommonJS transform (module_commonjs.rs) now delegate to the shared implementation. Also fixed a latent bug where `helpers.rs` didn't check `declare` modifier on nested `MODULE_DECLARATION` nodes. Net -73 lines.
36. ~~**Dead code: commented-out debug `println!`**~~ ✅ Done (4a69e301b) — removed 3 commented-out debug println statements from `solver/relations/subtype.rs`.
37. ~~**DRY violation: `get_range` + `get_identifier_text` in LSP**~~ ✅ Done (14963955b) — 4 identical `get_range` and 2 identical `get_identifier_text` implementations in `call_hierarchy.rs`, `type_hierarchy.rs`, `document_symbols.rs`, `linked_editing.rs` consolidated into shared `node_range()` and `identifier_text()` free functions in `utils.rs`. Net -9 lines.
38. ~~**DRY violation: `is_const_enum` + `is_ambient_enum` + `get_member_name` in tsz-emitter**~~ ✅ Done — replaced 4 copies of `is_const_enum` and 2 copies of `is_ambient_enum` with `NodeArena::has_modifier()` calls; consolidated 5 copies of `get_member_name` into shared `enum_member_name()` in `transforms/emit_utils.rs`. Net -108 lines. ~~Remaining emitter DRY violations: `get_module_specifier_text` (2 copies), `is_computed_property_member` (2 copies).~~ ✅ Done — consolidated into `module_specifier_text`, `is_computed_property_member`, `is_spread_element` free functions in `emit_utils.rs`. Net -29 lines. ~~Remaining: `has_modifier` raw loop (2 copies in declaration_emitter/helpers.rs + emitter/helpers.rs duplicating `NodeArena::has_modifier`).~~ ✅ Done — see item 41.
39. ~~**DRY violations remaining in tsz-lsp**: `is_import_keyword` (3 copies in file_rename.rs, document_links.rs, project.rs), `is_require_identifier` (3 copies same files)~~ ✅ Done (27140d99e) — consolidated into shared `is_import_keyword()` and `is_require_identifier()` free functions in `utils.rs`. Net -60 lines. Remaining: `get_range` in 4 LSP providers (call_hierarchy, type_hierarchy, document_symbols, linked_editing).
40. ~~**DRY violation: thin modifier wrappers duplicated across emitter files**~~ ✅ Done (eb07d35d2) — inlined 11 wrapper methods (`has_declare_modifier` ×4, `has_export_modifier` ×5, `has_default_modifier` ×2) across 6 emitter files into direct `NodeArena::has_modifier()` calls at ~90 call sites. Net -180 lines (wrapper defs removed, call sites slightly longer but clearer).
41. ~~**DRY violation: `has_modifier` raw loop wrappers in emitter + checker**~~ ✅ Done — removed 3 remaining `has_modifier` raw-loop wrapper methods (emitter/helpers.rs, declaration_emitter/helpers.rs, checker context/mod.rs) that duplicated `NodeArena::has_modifier`. All ~35 call sites across 12 files now use `self.arena.has_modifier(modifiers, SyntaxKind::X)` directly, eliminating the `as u16` casts. Net change: 3 method definitions deleted, all callers simplified.
42. ~~**Emitter `declarations.rs`** (2,746 LOC): exceeded 2000-line limit~~ ✅ Split (f7c8c53e0) — extracted namespace/module IIFE emission logic (~870 LOC) into `declarations_namespace.rs`, reducing `declarations.rs` to 1,799 LOC.
43. ~~**Emitter `module_emission.rs`** (2,302 LOC): exceeded 2000-line limit~~ ✅ Split — extracted import emission logic (~680 LOC) into `module_emission_imports.rs`, reducing `module_emission.rs` to 1,623 LOC.
44. ~~**Emitter `emitter/mod.rs`** (2,156 LOC): exceeded 2000-line limit~~ ✅ Split (bf1a99422) — extracted source file emission logic (~750 LOC) into `source_file.rs`, reducing `mod.rs` to 1,404 LOC.
45. ~~**Emitter `transforms/class_es5_ir.rs`** (2,120 LOC): exceeded 2000-line limit~~ ✅ Split — extracted member emission methods (`emit_methods_ir`, `emit_static_members_ir`, getter/setter builders, rest parameter lowering, method name IR, ~635 LOC) into `class_es5_ir_members.rs`, reducing `class_es5_ir.rs` to 1,486 LOC.
46. ~~**tsz-server `handlers_diagnostics.rs`** (5,392 LOC): nearly 3x the 2000-line limit~~ ✅ Split (ded34ae8e) — extracted `handle_get_code_fixes`, `handle_get_combined_code_fix`, and all code-fix helpers/free functions/tests (~5,032 LOC) into `handlers_code_fixes.rs`, reducing `handlers_diagnostics.rs` to 373 LOC. Remaining large tsz-server files: `handlers_code_fixes.rs` (5,032 LOC — the code-fix logic is inherently large but now properly isolated), `handlers_completions.rs`, `handlers_editing.rs`, `handlers_info.rs`.
47. **tsz-lsp/src**: ~~36~~ ~~35~~ ~~31~~ ~~28~~ 26 loose `.rs` files at top level — candidates for subdirectory grouping. ~~`project*.rs` (5 files, 6,754 LOC)~~ ✅ Done — moved into `project/` subdirectory. ~~navigation providers (`definition.rs`, `type_definition.rs`, `implementation.rs`, `references.rs`)~~ ✅ Done (d6a881cf9) — moved 4 files (3,697 LOC) into `navigation/` subdirectory. ~~symbol providers (`document_symbols.rs`, `workspace_symbols.rs`, `symbol_index.rs`)~~ ✅ Done — moved 3 files (1,815 LOC) into `symbols/` subdirectory; existing `symbols.rs` facade became `symbols/mod.rs` with submodule declarations and re-exports. ~~hierarchy providers (`call_hierarchy.rs`, `type_hierarchy.rs`)~~ ✅ Done — moved 2 files (1,998 LOC) into `hierarchy/` subdirectory. Existing subdirectories: `code_actions/`, `completions/`, `hierarchy/`, `navigation/`, `project/`, `resolver/`, `symbols/`. ~~Remaining grouping candidates: `resolver.rs` + `resolver_children.rs` (resolver pair, ~2,003 LOC)~~ ✅ Done — moved into `resolver/` subdirectory as `mod.rs` + `children.rs` (24 → 23 loose files). ~~hover providers (`hover.rs`, `hover_contextual.rs`, `hover_format.rs`)~~ ✅ Done (88ab879f5) — moved 3 files (2,298 LOC) into `hover/` subdirectory; eliminated `#[path]` attributes (22 loose files remaining). ~~rename providers (`rename.rs`, `linked_editing.rs`, `file_rename.rs`)~~ ✅ Done (0507fc21e) — moved 3 files (1,094 LOC) into `rename/` subdirectory; `rename.rs` became `rename/mod.rs` with submodule declarations (19 loose files remaining). Existing subdirectories: `code_actions/`, `completions/`, `hierarchy/`, `hover/`, `navigation/`, `project/`, `rename/`, `resolver/`, `symbols/`. ~~highlighting providers (`highlighting.rs`, `semantic_tokens.rs`)~~ ✅ Done (a963342df) — moved 2 files (1,731 LOC) into `highlighting/` subdirectory; `highlighting.rs` became `highlighting/document.rs`, `semantic_tokens.rs` became `highlighting/semantic_tokens.rs` (17 loose files remaining). Existing subdirectories: `code_actions/`, `completions/`, `hierarchy/`, `highlighting/`, `hover/`, `navigation/`, `project/`, `rename/`, `resolver/`, `symbols/`. ~~editor range providers (`folding.rs`, `selection_range.rs`)~~ ✅ Done (c56272132) — moved 2 files (825 LOC) into `editor_ranges/` subdirectory (15 loose files remaining). Existing subdirectories: `code_actions/`, `completions/`, `editor_ranges/`, `hierarchy/`, `highlighting/`, `hover/`, `navigation/`, `project/`, `rename/`, `resolver/`, `symbols/`. ~~editor decoration providers (`inlay_hints.rs`, `code_lens.rs`)~~ ✅ Done (82eccb60a) — moved 2 files (707 LOC) into `editor_decorations/` subdirectory (13 loose files remaining). Existing subdirectories: `code_actions/`, `completions/`, `editor_decorations/`, `editor_ranges/`, `hierarchy/`, `highlighting/`, `hover/`, `navigation/`, `project/`, `rename/`, `resolver/`, `symbols/`. Remaining grouping candidates: `signature_help.rs` (1,491 LOC, standalone), `formatting.rs` (849 LOC, standalone), `diagnostics.rs`/`jsdoc.rs` (shared checker-integration pair).
48. ~~**LSP `project_imports.rs`** (2,657 LOC): exceeded 2000-line limit~~ ✅ Split (8481733d6) — extracted module specifier resolution (~1,588 LOC) into `project_module_specifiers.rs`, reducing `project_imports.rs` to 1,097 LOC. ~~**LSP `project.rs`** (2,203 LOC): exceeded 2000-line limit~~ ✅ Split — extracted LSP feature dispatch methods (~542 LOC) into `project_features.rs`, reducing `project.rs` to 1,680 LOC. ~~**LSP `resolver.rs`** (1,991 LOC): 9 lines from 2000-line limit~~ ✅ Split (c11cf8f0c) — extracted AST child-iteration dispatch table (~1,220 LOC) into `resolver_children.rs`, reducing `resolver.rs` to 766 LOC. ~~`completions.rs` (1,954 LOC)~~ ✅ Split (fae0d9273) — extracted string literal completion logic (~413 LOC) into `completions/string_literals.rs`, reducing `completions.rs` to 1,541 LOC. ~~`completions.rs` grew back to 1,837 LOC~~ ✅ Split (c73b68d05) — extracted member completion logic (~938 LOC: dot-access property collection, namespace exports, object literal contextual completions, contextual type resolution, shared `make_checker` helper) into `completions/member.rs`, reducing `completions.rs` to 870 LOC. Remaining large LSP files: `definition.rs` (1,637 LOC), `project_operations.rs` (1,631 LOC).
49. ~~**LSP `code_action_provider.rs`** (2,892 LOC): exceeded 2000-line limit~~ ✅ Split (6ff82cf14) — extracted extract-variable refactoring (~816 LOC) into `code_action_extract.rs`, reducing `code_action_provider.rs` to 2,094 LOC. ~~Remaining: import management section (~1,170 LOC) is the next extraction candidate if the file needs to shrink further.~~ ✅ Done — extracted import management (removal, merging, insertion, usage classification, organize imports, missing import quickfixes) (~1,359 LOC) into `code_action_imports.rs`, reducing `code_action_provider.rs` to 815 LOC.
50. **Emitter near-threshold files**: ~~`emitter/declarations.rs` (1,967 LOC — 33 lines from limit)~~ ✅ Split — extracted class emission logic (~1,160 LOC of methods + ~387 LOC of tests) into `declarations_class.rs`, reducing `declarations.rs` to 414 LOC. ~~`lowering_pass.rs` (1,916 LOC)~~ ✅ Split (9834ab491) — extracted `visit_children` child-traversal dispatch (~530 LOC) into `lowering_pass_visit_children.rs`, reducing `lowering_pass.rs` to 1,385 LOC. ~~`declaration_emitter/mod.rs` (1,850 LOC)~~ ✅ Split — extracted interface declaration/member emission (~357 LOC) into `declaration_emitter/interfaces.rs`, reducing `mod.rs` to 1,493 LOC. ~~`declaration_emitter/helpers.rs` (1,794 LOC)~~ ✅ Split — extracted type syntax emission (`emit_type`, `emit_entity_name`, ~492 LOC) into `declaration_emitter/type_emission.rs`, reducing `helpers.rs` to 1,304 LOC. ~~`transforms/namespace_es5_ir.rs` (1,793 LOC)~~ ✅ Cleaned (3b0f1084e) — removed dead `NamespaceTransformContext` struct (zero callers, zero test refs, marked "legacy"), reducing to 1,254 LOC. ~~`transforms/async_es5_ir.rs` (1,783 LOC)~~ ✅ Split — extracted AST-to-IR conversion utilities (expression_to_ir, statement_to_ir, convert_object_properties, convert_template_expression, convert_function_expression, convert_arrow_function, operator text helpers, ~626 LOC) into `async_es5_ir_convert.rs`, reducing `async_es5_ir.rs` to 1,157 LOC. Also removed dead `emit_commonjs_export` wrapper (suppressed with `#[allow(dead_code)]`) from `module_emission.rs`, and removed blanket `#![allow(warnings)]` from `ts2300_tests.rs`. ~~`emitter/expressions.rs` (1,985 LOC — 15 lines from limit)~~ ✅ Split — extracted call expression emission (emit_call_expression, optional call/method lowering, argument emission, paren-position helpers, ~468 LOC) into `expressions_call.rs`, reducing `expressions.rs` to 1,517 LOC. ~~Grew back to 2,030 LOC~~ ✅ Split (d5e0b91b3) — extracted property/element access, optional chain downlevel, numeric double-dot disambiguation, `type_assertion_wraps_object_literal` (~680 LOC) into `expressions_access.rs`, reducing `expressions.rs` to 1,350 LOC.
51. ~~**Dead test file: `usage_analyzer_tests.rs`** (217 LOC): 11 hollow test stubs with zero assertions, never compiled due to `autotests = false`~~ ✅ Removed (65d6a7393). Also wired up 2 orphaned real test files (`comment_tests.rs`, `variable_declaration_emit_tests.rs`) that were never compiled despite having actual assertions. Added `[[test]]` entries in Cargo.toml. Net: -219 LOC dead code, +4 tests running.
52. **Suppressed checker calls in `types/type_checking.rs`**: lines 1335-1336 (`self.check_type_parameters`) and 1370-1371 (`self.check_type_element`) — calls commented out with variables renamed to `_params`/`_member` to suppress warnings. Indicates deferred work.
53. **Disabled TS2697 check in `ambient_signature_checks.rs`**: lines 508-515 — multi-line disabled block for async method Promise return type check, labeled "DISABLED: Causes too many false positives" with TODO.
54. ~~**DRY violation: `get_keyof_type`/`get_keyof_inner` triplicated + `get_lazy_def_id`/`get_def_id` triplicated in solver `type_queries`**~~ ✅ Done (399513ca7) — removed duplicate definitions from `data.rs` and `classifiers.rs`, renamed `get_keyof_inner` → `get_keyof_type` as canonical name, aliased `get_def_id` via `pub use`. Net -30 lines.
55. ~~**DRY violation: `is_type_parameter` has divergent semantics**~~ ✅ Done — see item 73.
56. ~~**DRY violation: `contains_type_parameters` / `contains_type_parameters_db`**~~ ✅ Done — removed duplicate `ContainsTypeChecker` struct and `contains_type_matching_impl` (~127 LOC) from `type_queries/data.rs`. The three `_db` functions now delegate to `visitor_predicates::contains_type_matching` with adjusted predicates. Also fixed latent bug where `data.rs` version hardcoded `ThisType | BoundParameter => true` in `check_key`, which incorrectly affected `contains_infer_types_db` and `contains_error_type_db` (not just the type-parameter query). Net -127 lines.
57. ~~**Dead code: `let _var = expr` suppressed computations in checker**~~ Done in three passes — Pass 1: removed 7 dead variables (dispatch.rs, class_implements_checker.rs ×2, control_flow.rs, import_declaration_checker.rs, member_access.rs, flow_graph_builder.rs). Pass 2 (f1ec6e008): removed 8 more across checker/solver/emitter: `_ctx_helper` ContextualTypeContext constructions (type_computation_call.rs ×2, type_computation_complex.rs), `_member_type` enum kind match (state_type_environment.rs), `_no_match_type` + dead FlowAnalyzer/NarrowingContext block (statement_callback_bridge.rs), `_is_abstract` (declaration_emitter/mod.rs), `_readonly` mapped tuple modifier (mapped.rs), `_symbol_ref` construction (compat.rs), `_shape` fetches (infer_bct.rs ×2). Pass 3 (3e73ff5e0): removed 7 more across checker/LSP/CLI — `_node_data` (reachability_analyzer.rs), `_node` (control_flow_narrowing.rs, linked_editing.rs), `_file` (project/features.rs), `_root` (project/mod.rs), `_kind` (highlighting.rs), `_project_id` (project_refs.rs). Remaining `let _` patterns are intentional side-effect calls (type resolution, diagnostic generation, RAII guards).
58. ~~**Disconnected module: `decorators.rs`** (335 LOC, 3 pub fns)~~ ✅ Removed (e6e656952) — `DecoratorChecker`, `check_decorator`, `check_class_decorators`, `get_decorator_info` had zero callers. Module + 83 LOC test file deleted. Actual decorator checking (`check_decorator_global_types` for TS2318) lives in `state_class_checking.rs`.
59. **Dead computation: `_merged_type` in `state_type_analysis_computed.rs:439`** — `merge_namespace_exports_into_object` called but result discarded. Comments say "Store in separate cache" but storage never implemented. Skipped because `get_type_of_symbol` calls inside have type-resolution side effects that may be needed.
60. ~~**Emitter lowering_pass file sprawl**: `lowering_pass.rs` + `lowering_pass_helpers.rs` + `lowering_pass_visit_children.rs` (~2,900 LOC) at top level~~ ✅ Done (918332b70) — moved into `lowering/` subdirectory as `mod.rs`, `helpers.rs`, `visit_children.rs`; eliminated `#[path]` attributes. Backward-compatible re-export shim at `crate::lowering_pass`. ~~`emit_context.rs` + `transform_context.rs` (context cluster, ~840 LOC)~~ ✅ Done (3b90b889f) — moved into `context/` subdirectory as `emit.rs`, `transform.rs`; backward-compatible re-export shims at `crate::emit_context` and `crate::transform_context`. ~~`source_writer.rs` + `printer.rs` (output cluster, ~990 LOC)~~ ✅ Done (d3654a4a6) — moved into `output/` subdirectory as `source_writer.rs`, `printer.rs`; all internal imports updated to `crate::output::` paths directly. No emitter top-level grouping candidates remain.
61. ~~**Misplaced utility: `safe_slice` module inside `printer.rs`**~~ ✅ Done (77c8633e7) — `safe_slice` (safe UTF-8 string slicing) was defined as a nested `pub mod` inside `printer.rs` but used by 15+ emitter files via `crate::printer::safe_slice`. Extracted to standalone `safe_slice.rs` module; all internal imports updated to `crate::safe_slice`; tests moved to dedicated `tests/safe_slice.rs`. Net -14 lines (removed re-export shim since no external consumers).
62. ~~**DRY violation: duplicated instantiation patterns in `instantiation/instantiate.rs`**~~ ✅ Done — extracted 6 helper methods on `TypeInstantiator`: `instantiate_properties` (3 copies → 1), `instantiate_index_signature` (2 copies → 1), `instantiate_type_params` (2 copies → 1), `instantiate_params` (2 copies → 1), `enter_shadowing_scope` + `exit_shadowing_scope` (3 copies → 1). Net -49 lines.
63. ~~**DRY violation: Object/ObjectWithIndex name resolution in `format.rs`**~~ ✅ Done — extracted `resolve_object_shape_name` helper returning `Option<String>`, eliminating identical 10-line symbol-name/def-name lookup blocks from both `Object` and `ObjectWithIndex` match arms.
64. ~~**DRY violation: signature formatting in `format.rs`**~~ ✅ Done — unified `format_signature_arrow` and `format_call_signature` into shared `format_signature` method with a `separator` parameter (`"=>"` for arrow types, `":"` for call signatures). `format_call_signature` now delegates to the shared method.
65. ~~**Dead emitter functions**: `exit_commonjs()` (context/emit.rs:171), `new_es5()`/`new_es6()` (emitter/mod.rs:457-472), `set_required_imports()` (declaration_emitter/mod.rs:256), `print_to_writer()` (output/printer.rs:174), `lower_and_print_with_source_map()` (output/printer.rs:307)~~ ✅ Done — all 5 functions removed (zero callers). Net -114 lines (including 2 dead safe_slice boundary functions and their test).
66. ~~**Dead emitter enum functions**: `get_enum_values()`, `check_const_enum_access()`, `is_symbol_const_enum()` (checker.rs), `try_inline_const_enum_element_access()`, `get_enum_names()` (transform.rs), plus dead `ConstEnumInliner` wrapper struct~~ ✅ Done — removed 6 dead functions, 1 dead struct, 1 unused import, 1 unused diagnostic code constant. Net -97 lines.
67. ~~**Dead safe_slice functions**: `next_boundary()` and `prev_boundary()` in `safe_slice.rs`~~ ✅ Done — included in item 65 cleanup.
68. ~~**LSP `hover.rs`** (1,966 LOC): 34 lines from 2000-line limit~~ ✅ Split (673ad122e) — extracted contextual type resolution helpers (~700 LOC) into `hover_contextual.rs` and string formatting utilities (~180 LOC) into `hover_format.rs`, reducing `hover.rs` to 1,078 LOC. Next large LSP files to monitor: `definition.rs` (1,637 LOC), `project_operations.rs` (1,631 LOC), `project/mod.rs` (1,685 LOC), `completions.rs` (1,565 LOC).
69. ~~**Emitter `module_emission.rs`** (1,902 LOC): approaching 2000-line limit~~ ✅ Split (b2aac7ce0) — extracted CommonJS export declaration handler (~520 LOC) into `module_emission_exports.rs`, reducing `module_emission.rs` to 1,310 LOC. Also deduplicated 2×8 identical export-modifier match arms in `file_is_module`/`should_emit_es_module_marker` into shared `statement_has_export_modifier` helper (~60 lines saved).
70. ~~**DRY violation: `format_object_shape_name` in solver `format.rs`**~~ ✅ Done — see item 63.
71. ~~**DRY violation: `assignment_affects_reference` vs `assignment_targets_reference_internal`**~~ ✅ Done (5d6f6dbd5) — two ~100-LOC functions in `control_flow_narrowing.rs` with ~90% identical logic (destructuring pattern traversal) consolidated into `assignment_matches_reference_core` with `check_property_access` parameter. Also removed duplicate PROPERTY_ASSIGNMENT block. Net -62 lines.
72. ~~**DRY violation: `emit_export_specifier` vs `emit_import_specifier`**~~ ✅ Done — consolidated 4 identical specifier-emission functions (2 in `declaration_emitter/exports.rs`, 2 across `module_emission.rs`/`module_emission_imports.rs`) into unified `emit_specifier` methods. Net -26 lines.
73. ~~**DRY violation: `is_type_parameter` divergent semantics**~~ ✅ Done (a20650029) — `visitor_predicates.rs` version (`TypeParameter | Infer`) kept as `is_type_parameter`; `type_queries` version (`TypeParameter | BoundParameter | Infer`) renamed to `is_type_parameter_like` to make the broader semantics explicit. Updated all 10 checker call sites and 2 test files.
74. ~~**Divergent `is_unit_type` semantics in solver**~~ ✅ Done (3fac9d17f) — two functions named `is_unit_type` had different semantics: `visitor_predicates` version (for O(1) identity optimization) included void/never/tuples but missed BOOLEAN_TRUE/FALSE; `type_queries::flow` version (for narrowing) included void/BOOLEAN_TRUE/FALSE but missed Enum/UniqueSymbol. Fix: renamed optimization predicate to `is_identity_comparable_type`; fixed flow.rs `is_unit_type` to match tsc's `TypeFlags.Unit = Enum | Literal | UniqueESSymbol | Nullable` (added Enum/UniqueSymbol, removed void). Updated TypeDatabase trait, TypeInterner cache, and all 10 solver callers.
75. ~~**DRY violation: `apparent_primitive_shape` duplicated between evaluation and relations**~~ ✅ Done (961786536) — two near-identical ~45-LOC methods (`evaluation/evaluate_rules/apparent.rs` and `relations/subtype_rules/intrinsics.rs`) consolidated into shared `apparent_primitive_shape` in `objects/apparent.rs` with `impl Fn` callback for method-type factory. Also removed dead `is_module_namespace_type_db` (100% duplicate of `is_module_namespace_type`, zero callers). Net -41 lines.
76. **DRY violation: `apparent_method_type` divergent semantics** — evaluator's `make_apparent_method_type` (`evaluation/evaluate_rules/apparent.rs:15`) creates methods with `...any[]` rest param; subtype checker's `make_subtype_method_type` (`relations/subtype_rules/intrinsics.rs:29`) creates methods with empty params. These model the same concept (apparent wrapper method on primitives) but produce different TypeIds. Needs investigation whether the divergence is intentional or a bug.
77. ~~**DRY violation: `_db` suffix predicates in `visitor_predicates.rs`** — 5 pairs of near-duplicate predicates (`is_literal_type`/`_db`, `is_function_type`/`_db`, `is_object_like_type`/`_db`, `is_empty_object_type`/`_db`). The `_db` versions unwrap through `ReadonlyType`/`NoInfer`/`TypeParameter` constraints, which is semantically different but not obvious from the naming.~~ ✅ Fixed — renamed to explicit `_through_type_constraints` predicates in `tsz-solver::visitor` and updated all call sites.
78. ~~**DRY violation: nullish-check inline duplication**~~ ✅ Done (f1dc7ca1c) — replaced 6 inline `matches!(type_id, TypeId::NULL | TypeId::UNDEFINED | TypeId::VOID)` with `TypeId::is_nullable()`. Removed `is_nullish_intrinsic` (duplicate of `is_nullable`), `type_contains_nullish` and `can_be_nullish` (pure aliases of `is_nullish_type`). 2 remaining sites (`intersection.rs`, `intrinsics.rs`) include additional variants (`NEVER`, `UNKNOWN`) so are intentionally different. Net -14 lines.
79. ~~**DRY violation: `find_common_base_type`/`get_base_type` duplicated**~~ ✅ Done (062cb3fc8) — extracted shared `find_common_base_type` into `utils.rs` with `get_base: impl Fn(TypeId) -> Option<TypeId>` closure. Both `expression_ops.rs` (literal widening) and `infer_bct.rs` (literal + nominal hierarchy) now call the shared function, each providing their own `get_base_type` logic. Net -23 lines.
80. ~~**DRY violation: `sanitize_module_name` + `get_string_literal_text` duplicated in CommonJS modules**~~ ✅ Done (4fc35d45b) — identical `sanitize_module_name` (26 LOC) and `get_string_literal_text` (8 LOC) in both `module_commonjs.rs` and `module_commonjs_ir.rs` consolidated into shared functions in `transforms/emit_utils.rs`. Updated 2 external callers. Net -33 lines.
81. ~~**DRY violation: `get_identifier_text` wrapper method duplicated in 8 emitter structs**~~ ✅ Done — removed 8 identical 1-line wrapper methods (`Printer`, `EnumTransformer`, `ES5ClassTransformer`, `ES5AsyncTransformer`, `ES5DestructuringTransformer`, `ES5SpreadTransformer`, `AsyncES5Transformer`, `EnumES5Transformer`) that delegated to `emit_utils::identifier_text_or_empty`. All ~45 call sites now call the free function directly.
82. ~~**DRY violation: `next_temp_var` triplicated**~~ ✅ Done (1e09637f3) — extracted `next_temp_var_name(&mut u32)` into `emit_utils.rs`. All three structs (`DestructuringState`, `ES5DestructuringTransformer`, `ES5SpreadTransformer`) now delegate to the shared function. Net -11 lines.
83. ~~**DRY violation: `block_is_empty` duplicated**~~ ✅ Done (1e09637f3) — extracted `block_is_empty(arena, body)` into `emit_utils.rs`. Both `Printer` and `AsyncES5Transformer` now use the shared function.
84. ~~**DRY violation: `is_await_expression` duplicated**~~ ✅ Done (1e09637f3) — extracted `is_await_expression(arena, idx)` into `emit_utils.rs`. Both `ES5AsyncTransformer` and `AsyncES5Transformer` now use the shared function.
85. ~~**DRY violation (checker): `is_strict_mode_for_node` duplicated**~~ ✅ Done — moved shared implementation to `CheckerContext::is_strict_mode_for_node` in `context/strict_mode.rs`. Both `CheckerState` and `DeclarationChecker` now delegate to the shared method. Extracted `statement_is_use_strict` and `block_has_use_strict` as module-private free functions. Net -95 lines.
86. ~~**DRY violation (checker): `is_namespace_declaration_instantiated` + `module_body_has_runtime_members` duplicated**~~ ✅ Done (5f96ec034) — added `NodeArena::is_namespace_instantiated()` in `tsz-parser/src/parser/node_access.rs`. Replaced 2 divergent implementations (~90 LOC each) in `type_checking_declarations.rs` (CheckerState) and `declarations_module.rs` (DeclarationChecker). Also fixed latent bug where DeclarationChecker copy missed `NAMESPACE_EXPORT_DECLARATION` handling. Net -53 lines.
87. ~~**DRY violation (checker): `is_immediately_invoked` triplicated**~~ ✅ Done — added `NodeArena::is_immediately_invoked()` in `tsz-parser/src/parser/node_access.rs`. Replaced 3 divergent implementations (flow_analysis_usage.rs, scope_finder.rs, parameter_checker.rs) with calls to the shared method. The unified version handles arbitrary paren nesting, both CALL_EXPRESSION and NEW_EXPRESSION, and has a guard limit. Net -69 lines.
88. ~~**DRY violation (cross-crate): `is_binding_pattern` duplicated**~~ ✅ Done — added `Node::is_binding_pattern()` to `tsz-parser` (node_access.rs). Replaced inline kind checks in checker (`control_flow_var_utils.rs`), emitter (`binding_patterns.rs`, `lowering/helpers.rs`), and parser (`state_statements.rs`, `node_access.rs::get_binding_pattern`). Net -10 lines.
89. ~~**DRY violation (solver): `def_store.get(def_id)` fallback pattern triplicated in `format.rs`**~~ ✅ Done — extracted `format_def_id(def_id, fallback_prefix)` helper. Replaced 3 identical nested `if let Some(def_store) { if let Some(def) { … } else { format!("X({})", id) } } else { format!("X({})", id) }` blocks in `TypeData::Lazy`, `TypeData::Application(Lazy)`, and `TypeData::Enum` arms. Also simplified `PropertyAccessEvaluator::with_resolver` to delegate to `Self::new(db)` (body was identical, `_resolver` param unused). Net -27 lines.
90. ~~**DRY violation (solver): `get_array_member_types` duplicated in `index_access.rs`**~~ ✅ Done (259a5a53b) — extracted `get_or_init_array_member_types` free function. Both `ArrayKeyVisitor` and `TupleKeyVisitor` now delegate to the shared function. Net -2 lines.
91. ~~**DRY violation (solver): `resolve_object_member(...).unwrap_or(PropertyNotFound)` repeated 7 times in `property.rs`**~~ ✅ Done (fa6bd0b7b) — extracted `resolve_object_member_or_not_found` helper method on `PropertyAccessEvaluator`. All 7 call sites now delegate to the shared method. Net -14 lines.
92. ~~**DRY violation (solver): `canonicalize_index_signatures` duplicated in `canonicalize.rs`**~~ ✅ Done (4936ac246) — extracted `canonicalize_index_signature` helper method. Both `canonicalize_object` and `canonicalize_callable` now delegate to the shared method. Net -21 lines.
93. ~~**CLI `driver_resolution.rs`** (2,390 LOC): exceeded 2000-line limit~~ ✅ Split (599871a58) — extracted emit/output logic (~464 LOC: `EmitOutputsContext`, `emit_outputs`, `write_outputs`, output path helpers, path normalization) into `driver_emit.rs`, reducing `driver_resolution.rs` to 1,936 LOC. `js_extension_for` tests moved to `driver_emit_tests.rs`. Shared `is_declaration_file` and `canonicalize_or_owned` remain in `driver_resolution.rs` (used by both modules).
94. **CLI oversized server files**: ~~`handlers_info.rs` (3,442 LOC)~~ ✅ Split (61d676eb0) — extracted quickinfo handler + all private helpers (~2,012 LOC) into `handlers_quickinfo.rs`, reducing `handlers_info.rs` to 1,441 LOC. ~~`handlers_completions.rs` (3,593 LOC)~~ ✅ Split (fc8d37c7b) — extracted class member snippet logic (~1,460 LOC: all `class_member_snippet_*` methods, fallback scanning, import synthesis, and tests) into `handlers_completions_snippets.rs`, reducing `handlers_completions.rs` to 2,132 LOC. ~~`handlers_info.rs` (2,858 LOC — grew back over limit)~~ ✅ Split (6e26c07ef) — extracted module alias/quoted specifier resolution subsystem (~1,301 LOC) into `handlers_info_alias.rs`, reducing `handlers_info.rs` to 1,557 LOC. ~~`handlers_quickinfo.rs` (2,011 LOC)~~ ✅ Split — extracted pure text processing helpers (~560 LOC: signature parsing, type normalization, parameter type extraction, offset helpers, and tests) into `handlers_quickinfo_text.rs`, reducing `handlers_quickinfo.rs` to 1,450 LOC. ~~`handlers_completions.rs` (2,132 LOC)~~ ✅ Split — extracted display parts rendering, signature help handler, and tokenization utilities (~700 LOC) into `handlers_completions_display.rs`, reducing `handlers_completions.rs` to 1,438 LOC. ~~`handlers_code_fixes.rs` (6,793 LOC)~~ Partially split (72cb4327b) — extracted JSDoc annotation/type normalization/unknown-conversion/minimal-edit helpers (~1,455 LOC) into `handlers_code_fixes_jsdoc.rs`, reducing to 5,338 LOC. Further split (8db79fee9) — extracted import rewriting/module specifier resolution/CommonJS rewriting/auto-import candidate collection (~1,400 LOC) into `handlers_code_fixes_imports.rs`, reducing to 3,935 LOC. Further split (6262a2363) — extracted test module (~1,570 LOC, 27 tests) into `handlers_code_fixes_tests.rs`, reducing to 2,368 LOC. ~~Still over limit; next candidate: implement-interface helpers + text parsing utilities (~790 LOC, lines 1577-2364).~~ ✅ Done — extracted pure utility functions (text parsing, identifier checking, interface/class analysis, import specifier parsing, JSDoc import helpers, module path resolution, wildcard matching, import candidate ranking, ~801 LOC) into `handlers_code_fixes_utils.rs`, reducing `handlers_code_fixes.rs` to 1,587 LOC. ~~Remaining oversized: `tests.rs` (4,560 LOC) — over the 2000-line limit.~~ ✅ Done — previous session created `tests_completions.rs` (33 tests) and `tests_navigation.rs` (17 tests) but never wired them as submodules or removed the duplicates from `tests.rs`, leaving 50 duplicate tests and the file at 4,697 LOC. Fix: added `#[path]` module declarations in `tests.rs`, removed all 50 duplicate test functions (~3,207 lines). Final `tests.rs`: 1,495 LOC. All 148 server tests pass with proper module paths (`tests::completions::*`, `tests::navigation::*`).
95. ~~**DRY violation: `canonicalize_or_owned` duplicated in `watch.rs`**~~ ✅ Done — removed private copy from `watch.rs`, now imports from `driver_resolution::canonicalize_or_owned`. Note: `src/config.rs` (root crate) has a third copy but is in a different crate; consolidation would require a public API change.
96. **DRY violation: `canonicalize_or_owned` in root crate `src/config.rs`** — third copy of the same function, identical to `driver_resolution::canonicalize_or_owned`. In a different crate (`tsz-core`), so consolidation requires making it a public export or moving to a shared utility crate.
97. ~~**DRY violation (solver): `optional_property_type` duplicated in 5 files**~~ ✅ Done (2de34aa2c) — four identical copies across `PropertyAccessEvaluator`, `TypeEvaluator`, `CallEvaluator`, and `InferenceContext` consolidated into shared `optional_property_type` and `optional_property_write_type` free functions in `utils.rs`. `SubtypeChecker`'s version intentionally differs (respects `exactOptionalPropertyTypes`) and remains independent. Net -7 lines.
98. ~~**DRY violation (emitter): operator-to-string match duplicated in 6 files**~~ ✅ Done (e82c8fcf6) — 6 binary-operator and 2 unary-operator match blocks (13–49 arms each) consolidated into shared `operator_to_str(op: u16) -> &'static str` in `transforms/emit_utils.rs`. Also fixed latent bug in `enum_es5.rs` where default arm returned `"+"` for unrecognized operators. Net -121 lines.
99. ~~**DRY violation (emitter): unary operator-to-string match duplicated**~~ ✅ Done (e82c8fcf6) — included in item 98 consolidation; both `get_unary_operator_text` and `get_prefix_operator` now delegate to the shared `operator_to_str`.
100. ~~**DRY violation (emitter): `is_valid_identifier_name` duplicated**~~ ✅ Done (85cdd662b) — consolidated into shared `is_valid_identifier_name` in `transforms/emit_utils.rs`. Removed copies from `emitter/mod.rs` and `lowering/helpers.rs`. Net -10 lines.
101. ~~**DRY violation (solver): noop `TypeResolver` impls repeated 3 times**~~ ✅ Done — removed 12 redundant `-> None` method overrides from `TypeInterner` (5 methods), `QueryCache` (5 methods), and `NoopResolver` (1 method). Only the required `resolve_ref` and methods with real implementations (`get_boxed_type`, `get_array_base_type`, `get_array_base_type_params`) remain. Net -33 lines.
102. **DRY violation (checker): `skip_parenthesized` reimplemented 3 times** — `control_flow_narrowing.rs:571` (also skips non-null/comma), `control_flow_condition_narrowing.rs:462` (bounded loop), `type_checking_queries.rs:251` (simple while loop). All structurally equivalent ancestor-unwrapping loops.
103. ~~**DRY violation (checker): `is_ambient_declaration` reimplemented 3 times**~~ ✅ Done (9737d0ddc) — added `NodeArena::get_declaration_modifiers` (generic modifier extraction for any declaration node) and `NodeArena::is_in_ambient_context` (ancestor walk checking AMBIENT flag + `declare` modifier + implicit ambient nodes). Replaced 4 divergent implementations across `declarations.rs`, `type_checking_declarations.rs`, `declarations_module_helpers.rs`, and LSP `definition.rs`. Net -153 lines.
104. ~~**DRY violation (checker): `get_property_name` reimplemented 3 times**~~ ✅ Done (0285a75d9) — extracted shared `get_literal_property_name` free function in `type_checking_queries.rs`. Replaced 4 divergent implementations (type_checking_queries, type_node, member_access, interface_type). Also eliminated buggy local `normalize_numeric_name` in member_access.rs that used `format!("{n}")` instead of `js_number_to_string` (missing scientific notation, `-0` normalization). Net -68 lines.
105. ~~**Potential bug: blanket `impl<T: TypeResolver + ?Sized> TypeResolver for &T`** only delegates 4 out of ~20 methods~~ ✅ Done (3929bc786) — all 21 trait methods now properly delegate via `(**self).method(...)`. Previously only 5 methods were forwarded; the remaining 16 silently returned `None`/`false` defaults.
106. ~~**DRY violation (solver): `unwrap_readonly_for_lookup` duplicate of `unwrap_readonly`**~~ ✅ Done (7cc614b5e) — removed identical `unwrap_readonly_for_lookup` from `type_queries/extended.rs` (duplicate of `unwrap_readonly` in `data.rs`). Updated checker query boundary to call canonical `unwrap_readonly`. Net -9 lines.
107. ~~**Dead code: `for_each_child_type_id` in solver visitors**~~ ✅ Done (c421893ba) — removed dead function (zero callers, identical to `for_each_child_by_id`). Also removed unused `_interner` parameter from `compute_template_expression_type` (function only inspects `TypeId` constants). Net -18 lines.
108. ~~**Emitter `expressions.rs`** (1,876 LOC): approaching 2000-line limit~~ ✅ Split (fbc609544) — extracted binary expression downlevel emission (exponentiation, logical assignment, nullish coalescing transforms, ~528 LOC) into `expressions_binary_downlevel.rs`, reducing `expressions.rs` to 1,348 LOC. ~~Next emitter files to monitor: `declarations_class.rs` (1,891 LOC)~~ ✅ Split — extracted 22 class emission tests (~624 LOC) into `tests/declarations_class.rs`, reducing to 1,268 LOC. Next emitter file to monitor: `ir_printer.rs` (1,728 LOC).
109. ~~**Dangling test module: `ts18046_unknown_type_tests`** in `checker/src/lib.rs`~~ ✅ Removed (2625afae8) — `#[cfg(test)] mod ts18046_unknown_type_tests` referenced `../tests/ts18046_unknown_type_tests.rs` which never existed. Broke `cargo nextest run` for the checker crate.
110. **Pre-existing test failure: `compile_ambient_external_module_without_internal_import_declaration_fixture`** — `crates/tsz-cli/src/../tests/driver_tests.rs:3513` expects no diagnostics but gets TS18046. Likely a regression from the TS18046 unknown-type checking work.
111. ~~**Pre-existing test failure: `test_recursive_generic_extension_uses_structural_expansion_not_variance_arg_check`**~~ ✅ Fixed — root cause: `subtype_cache.rs` skipped DefId-level cycle detection for same-base Application types (e.g., `IObservable<X>` vs `IObservable<Y>`). This prevented coinductive recursion termination when evaluating recursive generics like `IObservable<T> = {n: IObservable<T[]>}`, because each evaluation created unique TypeIds that bypassed the TypeId-level cycle detector. Fix: removed the same-base Application skip from DefId cycle detection. The guard properly enters/leaves within each `check_subtype` call, so same-base non-recursive comparisons (e.g., `Array<number>` vs `Array<string>`) still work correctly. +14 conformance tests.
112. ~~**CLI `driver_check.rs`** (1,943 LOC): approaching 2000-line limit~~ ✅ Split — extracted export hash computation, tslib helper detection, binder construction, pragma detection, parse diagnostic conversion, and tests (~1,030 LOC) into `driver_check_utils.rs`, reducing `driver_check.rs` to 918 LOC.
113. ~~**Dead macro rules: `primitive_visitor!` in solver `operations/binary_ops.rs`**~~ ✅ Done (68f697967) — removed unused `ref_conservative` and `match_unique_symbol` feature flags. Neither was invoked by any of the 4 visitor instantiations.
114. ~~**DRY violation (checker): `is_const_variable_declaration` duplicated**~~ ✅ Done — added `NodeArena::get_variable_declaration_flags()` and `NodeArena::is_const_variable_declaration()` in `tsz-parser/src/parser/node_access.rs`. Replaced 2 named method definitions + 5 inline const-check patterns across 9 checker files (`type_checking_declarations.rs`, `control_flow_condition_narrowing.rs`, `control_flow_type_guards.rs`, `flow_analysis_definite.rs`, `reachability_checker.rs`, `control_flow_var_utils.rs`, `assignment_checker.rs`, `state_property_checking.rs`, `state_checking.rs`). The shared method merges node flags with parent VARIABLE_DECLARATION_LIST flags. Net -109 lines.
115. ~~**DRY violation (emitter): `get_extends_class` duplicated**~~ ✅ Done (810cc8a1a) — extracted shared `get_extends_expression_index(arena, heritage_clauses)` into `emit_utils.rs`, returning the raw `NodeIndex` of the extends expression. Both `ES5ClassTransformer` and `ES5Transformer` now delegate to the shared function, each applying their own expression-to-IR conversion. Net -6 lines (combined with item 116).
116. ~~**DRY violation (emitter): `get_property_key` duplicated**~~ ✅ Done (810cc8a1a) — extracted shared `get_property_key(arena, idx, convert_computed)` into `emit_utils.rs`, taking a closure for the computed property expression converter. Both `AstToIr` and `ES5SpreadTransformer` now delegate to the shared function.
117. **Pre-existing test failures (4)**: `tsz-conformance::tsz_wrapper::tests::test_compile_no_errors` and `test_compile_simple_error` (both lib and bin targets) — failing on main branch as of 2026-02-23.
118. **Note: `extern crate self as tsz_checker` in lib.rs** — appears unused in production code but is required by unit test files included via `#[path = "../tests/..."]` that reference `tsz_checker::context::LibContext` etc. Do not remove.
119. ~~**Dead code: 8 unused methods in `checkers/enum_checker.rs`**~~ ✅ Done (c11e7af4e) — removed `is_const_enum_type`, `is_regular_enum_type`, `is_enum_member_type`, `get_enum_member_base_type`, `enum_members_compatible`, `is_enum_assignable_to`, `is_enum_access_allowed`, `get_enum_member_access_type`. Only `is_enum_type` and `is_boxed_primitive_type` had callers (from `type_computation_binary.rs`). Net -158 lines.
120. ~~**Dead code: solver `def.rs` DefinitionStore methods**~~ ✅ Done (fbeac8cf1) — removed 10 dead methods from `DefinitionInfo` (`with_implements`, `with_exports`, `get_export`) and `DefinitionStore` (`get_instance_shape`, `get_static_shape`, `get_implements`, `get_enum_members`, `set_exports`, `set_enum_members`, `all_ids`). All were scaffolded for an anticipated API surface that never materialized; zero callers confirmed across all crates and tests. Net -63 lines.
121. **Correction: solver `visitors/visitor.rs` traversal functions** — NOT dead. `for_each_child` is called by `for_each_child_by_id` (used in emitter's `UsageAnalyzer`) and by `walk_referenced_types` (used by `collect_lazy_def_ids`, `collect_enum_def_ids`, `collect_type_queries`). `get_kind_of` and `test_type` are test-only but part of the public visitor API. No action needed.
122. ~~**Dead code: solver `classes/class_hierarchy.rs` factory methods**~~ ✅ Done (9ef1b53e7) — removed `ClassTypeBuilder` struct, `create_instance_type`, `create_constructor_type`, private helpers (`get_properties_of_type`, `create_object_type`), and `would_create_inheritance_cycle`. All had zero callers. Remaining `merge_properties` is test-only and gated behind `#[cfg(test)]`. Removed `pub use classes::class_hierarchy::*` from `lib.rs`. Net -127 lines.
123. ~~**Dead code: 31 functions/enums across solver type_queries and checker**~~ ✅ Done (76bc6ebf0) — Solver: removed 6 classifier enums+functions (`BindingElementTypeKind`, `SymbolResolutionTraversalKind`, `ConstructableTypeKind`, `SpreadPropertyKind` + classify_* functions), 5 query helpers (`is_type_query`, `is_unique_symbol_type`, `get_symbol_ref_from_type`, `construct_to_call_callable`, `get_callable/function_shape_id`), and `display_type_data_direct`. Checker: removed 7 dead `constructor_checker` methods, 11 dead `symbol_resolver` methods, `has_call_signatures` boundary wrapper, dead `error_reporter` helpers, dead `type_computation_access::get_type_name`, and dead `reachability_analyzer` methods. Net -798 lines.
124. ~~**Emitter `emitter/statements.rs`** (2,061 LOC): exceeded 2000-line limit~~ ✅ Split — extracted 42 statement emission tests (~931 LOC) into `tests/statements.rs`, reducing `statements.rs` to 1,130 LOC. Follows existing pattern of `#[cfg(test)] #[path] mod tests` used by `declarations_class.rs`.
125. **Checker near-threshold files to monitor**: `type_checking_utilities_jsdoc.rs` (1,822 LOC), `type_checking_queries_class.rs` (1,751 LOC), `member_declaration_checks.rs` (1,728 LOC), `flow_graph_builder.rs` (1,676 LOC), `import_declaration_checker.rs` (1,616 LOC).
126. ~~**Dead code: solver `Canonicalizer::clear_cache` + 3 `TypeFactory` wrappers**~~ ✅ Done (f440f291c) — removed `Canonicalizer::clear_cache` (zero callers — canonicalizer is always created inline and discarded), `TypeFactory::object_with_flags` (checker uses `object_fresh`/`object`; flags variant only called via `db` in solver internals), `TypeFactory::bound_parameter` and `TypeFactory::recursive` (only created during canonicalization/caching via `db` directly). Net -21 lines.
127. **Dead code candidates (test-only): `RecursionGuard` introspection methods** — `scope`, `is_active`, `visiting_count`, `iterations`, `max_depth`, `max_iterations` in `recursion.rs` have zero production callers (only used in `recursion_tests.rs`). Similarly `DepthCounter::new`, `max_depth`, `mark_exceeded`, `reset`. Low priority — these are observability/test APIs for a utility type.
128. ~~**Pre-existing test failure: `ts2839_js_file_strict_eq_only`**~~ ✅ Fixed — second TS2839 emission site (object/array literal equality path at line 581) was missing the `is_js_file()` guard for loose equality that the first emission site (line 508) had. Added `!self.is_js_file() || is_strict_eq` guard to the second path.
129. ~~**Dead code + DRY: checker `FlowGraph::get_node`, `build_flow_graph`, thin `skip_parenthesized` wrappers**~~ ✅ Done (70a58cc6d) — removed 2 dead functions (`FlowGraph::get_node`, `FlowGraphBuilder::build_flow_graph`), inlined 2 trivial `skip_parenthesized` delegate wrappers (`skip_parenthesized_non_recursive`, `skip_parenthesized_expression`) at all call sites. Net -24 lines.
130. ~~**DRY violation (checker): `is_js_file` guard duplicated ~10 times**~~ ✅ Done (d978cd7aa) — added `CheckerContext::is_js_file()` as the canonical JS-file check; `CheckerState::is_js_file()` now delegates to it. Replaced 17 inlined `file_name.ends_with` patterns across 11 files. Removed duplicate `is_js_like_file()`. Fixed 4 latent bugs where incomplete checks only tested `.js/.jsx` and missed `.mjs/.cjs` files (property_access_type.rs, signature_builder.rs, import_checker.rs). Net -56 lines. Remaining: 4 sites in import/module checkers check a *different file's* name (import target), not the current file — those are correct as-is.
131. ~~**DRY violation (checker): "walk 4 parents for JSDoc" loop duplicated 3+ times**~~ ✅ Done (621bc7820) — extracted `try_jsdoc_with_ancestor_walk` helper in `type_checking_utilities_jsdoc.rs`. Replaced 3 identical ancestor-walk loops in `jsdoc_type_annotation_for_node`, `jsdoc_satisfies_annotation_for_node`, `has_jsdoc_override_tag`, plus refactored `find_jsdoc_for_function` to reuse it. Also removed a fully duplicated mapped type construction block (copy-paste error, ~60 LOC) and stale `tracing::debug!` calls. File reduced from 1,802 → 1,675 LOC. Net -127 lines.
132. **Pre-existing test failures (14): `tsz-core parallel::tests::*`** — all parallel/merge tests and `symbol_resolution_tests::test_symbol_resolution_global_array_with_libs` fail on main as of 2026-02-23.
133. ~~**Dead code: `get_lazy_export` + `get_lazy_enum_member` on `TypeResolver` trait**~~ ✅ Done — removed 2 scaffolded trait methods (zero callers, zero overrides) and their blanket `impl<T: TypeResolver + ?Sized>` delegates. Also removed dead `is_in_namespace_context` from checker (zero callers) + its 210-LOC test. Reduced `get_object_property_type` visibility from `pub` to private (only used within same impl block). Net -277 lines.
134. ~~**DRY violation (checker): `can_skip_base_instantiation` duplicated**~~ ✅ Done (06db41237) — identical function in `class_type.rs` and `class_type_constructor.rs` consolidated: made canonical copy `pub(super)`, replaced duplicate with import, removed duplicate test. Net -15 lines.
~~135. **DRY violation (checker): `has_static_modifier` wrapper duplicated 4 times** — `type_checking_queries.rs:121`, `flow_graph_builder.rs:1658`, `state_checking.rs:821` (`has_static_modifier_in_arena`), plus `binder/nodes/names.rs:482`. All wrap `arena.has_modifier(modifiers, SyntaxKind::StaticKeyword)`. ~55 call sites across checker — high churn to inline.~~ ✅ Done
~~136. **DRY violation (cross-crate): `get_visibility_from_modifiers` duplicated**~~ ✅ Done — moved `Visibility` enum from `tsz-solver/src/types.rs` to `tsz-common/src/common.rs` (re-exported from solver for backward compat). Added `NodeArena::get_visibility_from_modifiers()` in `tsz-parser/src/parser/node_modifiers.rs`. Both checker (`queries/core.rs`) and lowering (`lower/mod.rs`) now delegate to the shared parser method. Net -15 lines.
137. ~~**DRY violation (checker): `is_ambient_declaration` duplicated**~~ ✅ Done — added `CheckerContext::is_declaration_file()` (checks `.d.ts`/`.d.mts`/`.d.cts`) and `CheckerContext::is_ambient_declaration()` as canonical implementation. Both `CheckerState` and `DeclarationChecker` now delegate to the shared method. Also fixed redundant `.d.ts` check in `import_declaration_checker.rs:507` and latent bug where `.d.mts`/`.d.cts` files were not recognized as declaration files. Net -5 lines.
138. ~~**Dead code (solver): 7 dead functions + 2 visibility reductions**~~ ✅ Done (8e0ac233b) — removed `is_invariant_position`, `is_bivariant_position`, `validate_variance` (infer_resolve.rs), `map_success_type`, `success_type_or` (property.rs), `remove_definitely_falsy_types` + `is_always_falsy` (narrowing/utils.rs), `SubtypeExplainer::are_types_structurally_identical` method (subtype_explain.rs), `count_literal_members` + `count_literal_members_impl` (template_literal.rs). Reduced `get_contextual_signature_for_arity` and `extract_literal_strings` from `pub` to private. Net -182 lines.
139. ~~**Dead code (CLI): `build_solution` in `build.rs:20`**~~ ✅ Done (65f01f590) — removed `build_solution`, duplicate `find_tsconfig`, and cleaned up unused imports.
140. ~~**Dead code (CLI): `project_refs.rs` functions**~~ ✅ Done (65f01f590) — removed `validate_composite_project`, `get_declaration_output_path`, `resolve_cross_project_import`, `try_resolve_in_project`.
141. ~~**Dead code (CLI): `incremental.rs` scaffolding**~~ ✅ Done (65f01f590) — removed `ChangeTracker::affected_count`, `BuildInfoBuilder::from_existing`, `BuildInfoBuilder::set_file_emit`.
142. ~~**Dead enum variants (CLI): `FileInclusionReason::TypeReference` and `::TripleSlashReference`**~~ ✅ Done (65f01f590) — removed both dead variants and their Display match arms.
143. **Unparsed CLI args**: `jsx_factory`, `jsx_fragment_factory`, `jsx_import_source` (args.rs:67,71,75), `emit_decorator_metadata` (args.rs:98) — parsed by clap but never forwarded to `ResolvedCompilerOptions` in `apply_cli_overrides`.
144. ~~**Dead code (solver): 16 functions + 6 enums in `type_queries/extended.rs`**~~ ✅ Done — removed `is_boolean_literal`, `is_invalid_index_type`, `is_direct_type_parameter`, `get_type_param_default`, `get_widened_literal_type`, `create_boolean_literal_type`, `classify_spread_type`, `is_iterable_type_kind`, `get_iterable_element_type_from_db`, `classify_for_type_parameter_content`, `classify_for_type_depth`, `is_narrowable_type_key`, `classify_for_private_brand`, `classify_symbol_ref`, `get_object_property_type`, `get_function_return_type` (shadowed duplicate). Removed `SpreadTypeKind`, `TypeParameterContentKind`, `TypeDepthKind`, `PrivateBrandKind`, `SymbolRefKind` enums. Reduced `pub` → `pub(crate)` on 6 internal-only `utils.rs` functions. Net -370 lines; `extended.rs` reduced from 1,387 → 1,017 LOC.
145. ~~**Dead code (solver): 15 functions + 8 enums across `type_queries/` submodules**~~ ✅ Done (fb070c99b) — removed `classify_for_keyof`/`KeyOfTypeKind`, `classify_for_contains_traversal`/`TypeContainsKind`, `get_literal_value` (extended.rs); `classify_for_non_object`/`NonObjectKind`, `classify_for_property_presence`/`PropertyPresenceKind`, `classify_for_falsy_component`/`FalsyComponentKind`, `is_valid_for_in_target` (flow.rs); `is_object_type_with_shape`, `has_call_signatures` (data.rs); `classify_iterable_type`/`IterableTypeKind` (iterable.rs); `is_intrinsic_type`, `classify_for_constraint`/`ConstraintTypeKind`, `classify_for_property_lookup`/`PropertyLookupKind` (mod.rs); `classify_for_property_access`/`PropertyAccessClassification`, `get_lazy_if_def` (traversal.rs). Net -636 lines.
146. ~~**Dead code (solver): 7 functions + 3 enums + 1 struct + 1 diagnostic code**~~ ✅ Done — removed `classify_for_new_expression`/`NewExpressionTypeKind`, `classify_for_construct_signature`/`ConstructSignatureKind` (extended_constructors.rs); `get_string_literal_atom`, `get_boolean_literal_value`, `get_callable_type_param_count`, `classify_type_parameter`/`TypeParameterKind` (extended.rs); `TypeCollectorVisitor` struct (visitors/visitor.rs); `DiagnosticBuilder::implicit_any_variable` method + unused `IMPLICIT_ANY` diagnostic code alias (diagnostics/). Reduced `classify_for_abstract_constructor` from `pub` to `fn` (internal-only). Net -316 lines.
147. **Dead code candidates (test-only): solver visitor functions** — `test_type`, `collect_all_types`, `collect_referenced_types`, `is_type_kind` in `visitors/visitor.rs` have zero or test-only callers. `RecursiveTypeCollector` used internally by `collect_all_types`. Low priority.
148. **Dead code candidates (solver): `unsoundness_audit.rs` methods** — `all_rules`, ~~`blocked_rules`~~, `count_by_status`, `overall_completion` are `pub` but only called internally. Could reduce visibility. Low priority. (`blocked_rules` removed in item 151.)
149b. **Dead code (emitter): scaffolded structs never used in production** — `ES5ClassTransformer`/`ES5AsyncTransformer` in `transforms/es5.rs` (~840 LOC, test-only), `EnumTransformer`/`EnumChecker` in `enums/` (test-only), `ArrowTransformContext` in `transforms/arrow_es5.rs` (test-only), `LoopCaptureInfo`/`analyze_loop_capture`/`collect_loop_vars` in `transforms/block_scoping_es5.rs`. All have zero production callers.
~~149c. **Dead code (emitter): `HelpersNeeded` fields never set to true**~~ Partially done — removed 7 completely dead fields (`spread`, `spread_arrays`, `await_values`, `async_generator`, `async_delegator`, `set_function_name`, `prop_key`) that had no emit block, no helper constant, and were never set. Kept 3 scaffolded fields (`param`, `metadata`, `class_private_field_in`) that have complete infrastructure (helper constant + emit block) but no lowering pass triggers them yet — these represent real unimplemented features (decorator metadata, private-field-in lowering). Original doc incorrectly listed `decorate`, `read`, `async_values` as dead; they ARE set in lowering passes. Net -7 fields.
~~149d. **Dead code (emitter): `EmitContext` fields/methods**~~ ✅ Done (50aae56ec) — removed `needs_es2024_lowering`, `needs_es2025_lowering` (set but never read), `module_temp_counter` (never read), `enter_function_scope`/`enter_class_scope`/`temp_var_counter`/`set_temp_var_counter` (zero production callers, callers access sub-structs directly). Net -46 lines.
~~149e. **Dead code (emitter): `safe_slice` functions**~~ ✅ Done (50aae56ec) — removed `slice_from`, `slice_to`, `char_at`, `byte_at` (zero production callers, only `slice()` used). Net -44 lines + 24 lines of dead tests.
~~149f. **Dead code (emitter): IRNode/IRProperty builders**~~ ✅ Done (50aae56ec) — removed `IRNode::new_expr`, `IRNode::object_with_source`, `IRProperty::init_string`/`getter`/`setter` (zero production callers). Net -44 lines.
~~149g. **Dead code (checker): `_type_id` unused parameter**~~ ✅ Done (9be6e1baf) — removed unused `_type_id: TypeId` parameter from `check_always_truthy` (now in `types/queries/core.rs`). TS2872 check is purely syntactic; updated 1 call site in `computation/binary.rs`.
149. **Pre-existing CI failures**: `tsc_compat_tests` (13 tests) failing across all recent runs. Not related to architecture work.
150. ~~**Dead code (emitter): 12 speculative API methods**~~ ✅ Done (cbd4341cc) — removed `EmitFlags::new()`/`async_context()`/`generator_context()`, `ArrowTransformState::enter_arrow_with_this()`/`exit_arrow_with_this()`/`is_capturing_this()`/`mark_this_captured()`/`is_this_captured()`, `EnumValue::is_number()`/`is_string()`/`as_number()`/`as_string()`. All zero production callers. Net -96 lines.
151. ~~**Dead code (solver + checker): WellKnownSymbolKey enum + 11 dead functions**~~ ✅ Done — removed `WellKnownSymbolKey` enum (76 LOC, zero references), `probe_assignability_cache` (query_cache.rs), `merge_from`/`transitive_reduction`/`add_property_candidate`/`collect_constraint` (infer.rs), `with_max_depth` (visitor.rs), `with_tracer` (subtype.rs), `build_object_with_index` (literal.rs), `for_yield` (contextual/mod.rs), `lawyer_mut` (compat.rs), `blocked_rules` (unsoundness_audit.rs), `has_promise_in_lib` (checker lib_queries.rs). Also removed unused `ObjectFlags` import. Net -230 lines.
152. ~~**Checker `types/` directory sprawl**: 38 files at top level~~ ✅ Partially done — moved 9 `type_computation_*.rs` files (7,864 LOC) into `types/computation/` subdirectory with shorter names (`helpers`, `access`, `binary`, `call`, `call_helpers`, `complex`, `identifier`, `object_literal`, `tagged_template`). Updated `types/mod.rs`, `lib.rs` re-exports, architecture contract tests, inter-module `super::` references, and doc comments. ~~Remaining grouping candidates: `type_checking_utilities_*.rs` (4 files)~~ ✅ Done (a810e0347) — moved 4 files (4,487 LOC) into `types/utilities/` subdirectory (`mod.rs`, `enum_utils.rs`, `jsdoc.rs`, `return_type.rs`). Remaining grouping candidates: `type_checking_queries_*.rs` (6 files in `types/queries/` — already done).
153. ~~**Checker `state/` directory sprawl**: `state_variable_checking_*` files~~ ✅ Done (f2549eb64) — moved 3 `state_variable_checking_*` files (2,401 LOC) into `state/variable_checking/` subdirectory (`core.rs`, `destructuring.rs`, `for_loop.rs`). Updated `state/mod.rs` and architecture contract test file paths. ~~Remaining grouping candidates: `state_type_analysis_*` (4 files, 3,907 LOC)~~ ✅ Done (37da4fbf8) — moved into `state/type_analysis/` subdirectory (`mod.rs`, `computed.rs`, `computed_helpers.rs`, `cross_file.rs`). ~~Remaining grouping candidates: `state_type_environment*` (2 files, 1,961 LOC)~~ ✅ Done (5fbc8f157) — moved into `state/type_environment/` subdirectory (`mod.rs`, `lazy.rs`). No remaining `state_*` file families to group.
154. ~~**Dead code: `ref_symbol()` always returns `None` + 15 dead branches**~~ ✅ Done (1c1e9b928) — after TypeData::Ref → Lazy(DefId) migration, `ref_symbol()` could only return `None`. Removed the function, 3 dead methods (`check_ref_ref_subtype`, `check_ref_subtype`, `check_to_ref_subtype`), the `seen_refs: FxHashSet<(SymbolRef, SymbolRef)>` field from `SubtypeChecker`, and 15+ unreachable `if let Some(sym) = ref_symbol(...)` branches across 6 files. Net -275 lines.
155. ~~**Dead code candidates (checker): zero-caller functions in `subtype_identity_checker.rs`**~~ ✅ Done (9be6e1baf) — removed `are_types_identical`, `is_assignable_to_union`, `is_subtype_of_with_env` (all zero production callers). Cascade: also removed `is_assignable_with_resolver` from `query_boundaries/assignability.rs` (only caller was removed `is_assignable_to_union`). Updated architecture contract test boundary marker.
156. ~~**Dead code candidates (checker): `is_fuel_exhausted` in `context/mod.rs`**~~ ✅ Done (9be6e1baf) — removed `is_fuel_exhausted` method + `fuel_exhausted: RefCell<bool>` field + 6 constructor initializations. `consume_fuel()` return value already communicates exhaustion state.
157. ~~**Dead code candidates (binder): `inject_lib_symbols` in `state_lib_merge.rs`**~~ ✅ Done (9be6e1baf) — removed 1-line delegate wrapper (zero callers). `resolved_identifier_cache_len` kept as test-only introspection method (used by `binder_state_tests.rs`).
158. ~~**DRY violation: `is_callable_type` triplicated**~~ ✅ Done (a8dfbe5f3) — removed duplicate `is_callable_type` methods from `Canonicalizer` and `TypeInterner`; 6 call sites in `canonicalize.rs`, `intern/mod.rs`, and `intern/intersection.rs` now delegate to canonical `type_queries::is_callable_type`. The semantically different variant in `intrinsics.rs` (handles sentinels, unions, intersections) remains independent. Also consolidated `resolve_ref_type`/`resolve_lazy_type` (identical functions with different names, leftover from Ref→Lazy migration): removed `resolve_ref_type`, updated 8 callers across `subtype_helpers.rs`, `subtype_overlap.rs`, `subtype_explain.rs`, `compat.rs`. Net -29 lines.
159. ~~**DRY violation: `is_declaration_file` divergent**~~ ✅ Done (77b512d17) — consolidated 15 inlined `file_name.ends_with(".d.ts")` checks across 10 checker files into `CheckerContext::is_declaration_file()`. Fixed latent bugs where `.d.mts`, `.d.cts`, and `.d.tsx` files were not recognized as declaration files. Added `is_declaration_file_name()` free function for checking arbitrary file names. Canonical method now checks all four extensions (`.d.ts`, `.d.tsx`, `.d.mts`, `.d.cts`). Remaining: `driver_resolution.rs:1924` (different crate, checks `Path` not `&str`).
160. **Unwired LSP feature: `DocumentLinkProvider`** — fully implemented in `tsz-lsp/src/document_links.rs` (245 LOC) with tests, but no `textDocument/documentLink` handler in `tsz-server/main.rs`.
161. ~~**Misplaced file: `type_checking_class_helpers.rs` in `types/`**~~ ✅ Done (57dac55f8) — moved to `classes/class_helpers.rs`. All 6 methods are class AST inspection helpers (class_has_base, class_extends_null, find_constructor_body, etc.) called from `classes/super_checker.rs` and `state/state_class_checking.rs`.
162. ~~**Checker root-level file candidates for grouping**: `judge_integration.rs` + `optional_chain.rs`~~ ✅ Done (6b9f0d8bb) — `optional_chain.rs` (40 LOC, 1 fn) absorbed into `types/computation/access.rs` (co-located with primary consumer); `judge_integration.rs` (37 LOC, 2 methods) moved to `state/type_resolution/judge.rs` (co-located with type resolution operations). Also removed dead test file `tests/optional_chain.rs` (referenced nonexistent `analyze_optional_chain`, never compiled). Checker root reduced from 11 → 9 loose files.
163. ~~**Checker `types/` remaining organizational candidates**: 7 `type_checking_*.rs` files~~ ✅ Done (51f4a71ad) — moved into `types/type_checking/` subdirectory: `type_checking.rs` → `core.rs`, `type_checking_declarations.rs` → `declarations.rs`, `type_checking_declarations_utils.rs` → `declarations_utils.rs`, `type_checking_duplicate_identifiers.rs` → `duplicate_identifiers.rs`, `type_checking_global.rs` → `global.rs`, `type_checking_property_init.rs` → `property_init.rs`, `type_checking_unused.rs` → `unused.rs`. Matches organizational pattern of `computation/`, `queries/`, `utilities/` in same directory.
164. **Checker `state/` naming inconsistency**: 5 files inside `state/` have redundant `state_` prefix (`state_checking.rs`, `state_class_checking.rs`, `state_directive_checking.rs`, `state_heritage_checking.rs`, `state_property_checking.rs`) plus the `state_checking_members/` subdirectory. Would require updating ~80 references including architecture contract test file paths and crate-root re-exports. Moderate churn.
165. **DRY violation (solver): `resolve_ref_type`/`resolve_lazy_type`/`resolve_type` pattern repeated in 4 files** — core pattern `if let Some(def_id) = lazy_def_id(interner, type_id) { resolver.resolve_lazy(def_id, interner).unwrap_or(type_id) } else { type_id }` exists in `subtype.rs` (now canonical `resolve_lazy_type`), `objects/collect.rs:112` (`resolve_type` generic over TypeResolver), and `narrowing/mod.rs:341` (with caching layer). The `objects/collect.rs` and `narrowing` versions could potentially delegate to a shared free function.
166. **DRY violation (solver): `collect_object_spread_properties` diverged implementations** — `caches/query_cache.rs` has its own full implementation with caching and visited set (lines 271-330), while `caches/db.rs` delegates to `ObjectLiteralBuilder` in `objects/literal.rs`. Two independent code paths can produce different results. Correctness risk.
167. **DRY violation (solver): `is_object_like`/`is_object_type` 4 variants with divergent semantics** — `visitor_predicates.rs:59` (`is_object_like_type`), `visitor_predicates.rs:450` (`is_object_like_type_db`), `subtype_overlap.rs:387` (narrower, checks only `object_shape_id`/`object_with_index_shape_id`/`intersection_list_id`), `type_queries/mod.rs:153` (`is_object_type`). Subtly different variant sets; maintenance hazard.
~~168. **DRY violation (checker): modifier wrappers duplicated outside `queries/core.rs`**~~ ✅ Done — inlined `has_readonly_modifier` from `type_node.rs` (3 call sites → `self.ctx.arena.has_modifier(...)`), `has_declare_modifier_in_arena` from `flow_analysis_usage.rs` (2 call sites → `decl_arena.has_modifier(...)`). Also inlined `has_readonly_modifier`, `has_const_modifier`, `get_visibility_from_modifiers` from `lowering/mod.rs` (6 call sites → `self.arena.*`), and `has_declare_modifier` from `binder/nodes/names.rs` (3 call sites → `arena.has_modifier_ref(...)`). Net: 6 wrapper methods removed, 14 call sites inlined. Canonical wrappers in `queries/core.rs` retained.
~~169. **DRY violation (checker): `is_in_*` ancestor-walk pattern in `classes/super_checker.rs`**~~ ✅ Partially done (089563daf) — extracted `is_in_class_member_of_kind` shared helper for the 3 identical methods (`is_in_class_method_body`, `is_in_class_accessor_body`, `is_in_class_property_initializer`). These share the same "skip arrows, stop at functions, match target kinds" pattern. Net -68 lines. Remaining 4 methods (`is_in_class_static_block`, `is_in_constructor`, `is_in_constructor_parameter_context`, `is_in_object_literal_member`) have different traversal semantics (no arrow skip, extra state tracking, etc.) and aren't easily unified.
170. **CLI near-threshold files**: `driver_resolution.rs` (1,946 LOC), `handlers_info.rs` (1,851 LOC), `driver.rs` (1,776 LOC), `handlers_editing.rs` (1,774 LOC). Also `tests_completions.rs` (2,225 LOC) is over the 2000-line limit but is a pure test file.
171. ~~**Parser `node_access.rs`** (2,868 LOC): exceeded 2000-line limit~~ ✅ Split (df98f0ac9) — extracted 11 `collect_*_children` methods + 3 helper functions (~791 LOC) into `node_children.rs`, reducing `node_access.rs` to 2,077 LOC. Cleaned up 87 unused syntax_kind_ext imports. Remaining `node_access.rs` sections: typed getters (~1,095 LOC), `define_at_accessors!` macro (~95 LOC), NodeView wrapper (~135 LOC), Node kind predicates (~85 LOC), NodeInfo/NodeAccess trait (~120 LOC). Next extraction candidate: typed getters into `node_typed_getters.rs`.
172. **Parser over-2000 LOC files**: `scanner_impl.rs` (2,891 LOC), `node_arena.rs` (2,244 LOC), `state.rs` (2,282 LOC), `state_expressions_literals.rs` (2,113 LOC), `state_expressions.rs` (2,006 LOC). Not covered by the checker-specific 2000 LOC rule but exceed the general maintainability threshold.
173. ~~**DRY violation: `build_for_in_statement` / `build_for_of_statement` 100% identical in `flow_graph_builder.rs`**~~ ✅ Done — consolidated into single `build_for_in_of_statement`. Net -41 lines.
174. ~~**DRY violation: `emit_type_arguments` pattern triplicated in `declaration_emitter/type_emission.rs`**~~ ✅ Done — extracted `emit_type_arguments` helper method. Net -18 lines.
175. ~~**DRY violation: inline `error_if_ts_only_type_params` in `state_class_checking.rs`**~~ ✅ Done — replaced with call to existing shared helper in `state_checking.rs`.
~~176. **DRY violation: `emit_get_accessor` / `emit_set_accessor` share identical 14-line function-body-emit block**~~ ✅ Done (f8306158c) — extracted `emit_accessor_body` helper. Net -27 lines (combined with parser fix).
~~177. **DRY violation: `transform_nested_namespace_with_var` vs `transform_nested_namespace_exported_with_var` near-identical**~~ ✅ Done (734e5c7df + 46e5149bf) — consolidated 4 pairs of exported/non-exported namespace transform functions into unified versions with `force_export: bool` parameter: `transform_function_in_namespace`+`_exported`, `transform_class_in_namespace`+`_exported`, `transform_enum_in_namespace`+`_exported`, `transform_nested_namespace_with_var`+`_exported_with_var` → `transform_nested_namespace_core`. Net -96 lines. Follow-up (46e5149bf): consolidated remaining pairs missed in first pass: `transform_variable_in_namespace`+`transform_variable_exported` → unified with `force_export`, `transform_namespace_member`+`transform_namespace_member_exported` → unified dispatcher with `force_export`. Also removed dead `transform_nested_namespace`/`_exported` thin wrappers. Net -35 lines.
178. **DRY violation: `get_range` 1-line wrapper duplicated in 4 LSP files** — `rename/linked_editing.rs`, `symbols/document_symbols.rs`, `hierarchy/type_hierarchy.rs`, `hierarchy/call_hierarchy.rs`. All delegate to `node_range()` from `utils.rs`. Could inline at call sites.
~~179. **DRY violation: parser accessor body-parsing block duplicated**~~ ✅ Done (f8306158c) — extracted `parse_accessor_body` helper in `state_statements_class_members.rs`.
180. ~~**DRY violation: `is_this_type` and `is_error_type` duplicated in `type_queries/mod.rs`**~~ ✅ Done (3da37b5fc) — removed duplicate definitions from `type_queries/mod.rs` (these were missed during item-33 consolidation). Merged `TypeId::ERROR` fast-path from the `type_queries` version into the canonical `visitor_extract` version. Updated 1 checker caller. Net -11 lines.
181. **DRY violation: `is_object_keyword_type` structural copy-paste** — `relations/subtype_rules/intrinsics.rs:127` and `inference/infer_bct.rs:516` share ~20 lines of identical union/intersection recursion + shape checks, differing only in `any` handling. Could extract shared structural body with `any_handler` parameter.
182. **Dead in production: `is_module_namespace_type`** — `visitors/visitor_predicates.rs:32`, exported via `lib.rs` but zero production callers (only used in `visitor_tests.rs`). Low priority.
183. **Test-only constructors: `RecursionGuard::new` and `RecursionGuard::scope`** — `recursion.rs:278,378`. All production code uses `::with_profile()`. The `new`/`scope` methods are only used in `recursion_tests.rs`.
184. **DRY violation (emitter): `emit_method_modifiers_js` / `emit_class_member_modifiers_js` in `declarations_class_members.rs`** — lines 225-245 and 296-313 share ~17 identical lines of modifier iteration (decorator emission, static keyword, then differ only on `AsyncKeyword` vs `AccessorKeyword`). Could extract shared helper with optional keyword parameter.
185. **Dead code (solver): standalone `infer_call_signature` free function** — `operations/mod.rs:1022`. Only called from `operations_tests.rs`. The method version on `CallEvaluator` (line 348) is the one used in production.
186. **Dead code (solver): `process_template_escape_sequences`** — `types.rs:1157`. Zero production callers; only called from 20 tests in `types_tests.rs`. Escape processing for template literals; may be scaffolded for future use.
187. **DRY violation (emitter): `transform_import_equals_in_namespace` / `transform_import_equals_exported` divergent logic** — `namespace_es5_ir.rs:870-929`. Not a simple `force_export` consolidation: the exported version has additional qualified-name validation (type-only namespace skip) that the non-exported version lacks. Needs investigation whether the divergence is intentional or a bug.
188. ~~**DRY violation (solver): `expand_tuple_rest` + `TupleRestExpansion` triplicated**~~ ✅ Done (a259edc94) — three identical copies (struct + method) in `operations/call_args.rs`, `inference/infer_bct.rs`, `relations/subtype_rules/tuples.rs` consolidated into shared `expand_tuple_rest` free function + `TupleRestExpansion` struct in `utils.rs`. All three `impl` blocks now delegate to the shared function. Net -71 lines.
189. **DRY violation (solver): `rest_element_type` duplicated** — `operations/call_args.rs:265` and `inference/infer_bct.rs:764` share ~15 identical lines; `infer_bct.rs` version adds an early `TypeId::ANY` return. Could extract shared function with optional `any` guard.
190. **DRY violation (solver): `optional_property_type` divergent in `subtype_rules/objects.rs:873`** — respects `exactOptionalPropertyTypes` flag unlike canonical `utils.rs:227`. The flag is silently not applied in inference (`infer_bct.rs` delegates to canonical). Potential correctness divergence.
191. **DRY violation (solver): `evaluate_type` local cache in `subtype_rules/functions.rs:1069`** — maintains struct-level evaluation cache that bypasses solver's canonical `QueryCache` memoization. Architecturally inconsistent.
192. ~~**DRY violation (solver): `rest_element_type` + `tuple_element_type` duplicated within `index_access.rs`**~~ ✅ Done (3adb39125) — identical implementations on `TupleKeyVisitor` and `TypeEvaluator` (both handle arrays, nested tuples, optional elements) consolidated into shared `rest_element_type_full` and `tuple_element_type_with_rest` free functions taking `&dyn TypeDatabase`. Also removed dead `TypeEvaluator::tuple_element_type` wrapper (zero callers) and dead `TupleKeyVisitor::rest_element_type` wrapper. Net -23 lines.
193. ~~**DRY violation (checker): `contains_type_parameters` duplicate definition in `query_boundaries/assignability.rs`**~~ ✅ Done — `assignability.rs:26` had byte-for-byte duplicate of `common.rs:25`. Replaced with `pub(crate) use super::common::contains_type_parameters;` re-export. The `common.rs` module docstring explicitly states it is the canonical location for shared wrappers.
194. **Query boundary bypass: `contains_type_parameters_db` called directly from solver** — Three checker modules bypass `query_boundaries/` and call `tsz_solver::type_queries::contains_type_parameters_db` directly: `error_reporter/call_errors.rs:455`, `error_reporter/properties.rs:40`, `assignability/assignment_checker.rs:615`. Should route through appropriate boundary modules.
~~195. **DRY violation (checker): `is_assignable_to_*` cache-setup skeleton repeated 4x**~~ ✅ Partially done (f3eada257) — extracted `check_assignability_cached` (cache lookup → compute → cache insert → trace) and `prepare_assignability_inputs` (resolve+substitute+evaluate) shared helpers. `is_assignable_to_strict` and `is_assignable_to_strict_null` reduced to 4-line delegates. `is_assignable_to` uses the shared core with extra pre/post checks. `is_assignable_to_bivariant` remains independent (different compute function, no ThisType substitution, flags use `& !` instead of `|`). Net -67 lines.
196. **DRY violation (solver): union-binding-merge loop in `infer_pattern_helpers.rs`** — ~10 occurrences of identical 10-line `FxHashMap` + union merge loop, differing only in which `match_*` call fills member bindings. Could extract `merge_union_member_bindings` helper to remove ~100 lines.
197. ~~**DRY violation (emitter): `skip_trivia_forward` duplicated**~~ ✅ Done — `emitter/helpers.rs:892` and `transforms/namespace_es5_ir.rs:150` had near-identical ~35-LOC implementations of whitespace/comment skipping. Extracted shared `skip_trivia_forward(source_text, start, end)` free function in `transforms/emit_utils.rs`. Both methods now delegate to the shared function. Net -30 lines.
198. **DRY violation (emitter): `contains_await_recursive` duplicated** — `transforms/async_es5_ir.rs:688` (364 LOC) and `transforms/es5.rs:1279` (67 LOC) both implement recursive await detection in AST subtrees, avoiding nested function boundaries. The async_es5_ir version is much more comprehensive. Could extract shared utility.
199. ~~**DRY violation (solver): `any_args_function` duplicated `make_apparent_method_type`**~~ ✅ Done (1ad68c328) — `operations/property_helpers.rs:318` had identical body to `evaluation/evaluate_rules/apparent.rs:15` (`make_apparent_method_type`). Replaced with delegation. Net -14 lines.
200. ~~**DRY violation (solver): local `get_object_shape` duplicated `get_object_shape_id`**~~ ✅ Done (1ad68c328) — `relations/subtype_rules/unions.rs:243` redefined `get_object_shape(db, type_id) -> Option<ObjectShapeId>` using `object_shape_id || object_with_index_shape_id`, identical to public `type_queries::data::get_object_shape_id`. Replaced 3 call sites with the public version.
201. ~~**DRY violation (solver): `is_member` trivial helper duplicated**~~ ✅ Done (8a8559af6) — made `is_member` `pub(crate)` in `objects/apparent.rs`, replaced duplicate in `evaluation/evaluate_rules/index_access.rs` with import.
202. **DRY violation (solver): `is_type_reference` divergent semantics** — `visitors/visitor_predicates.rs:167` matches `Lazy|Recursive`; `type_queries/mod.rs:170` matches `Lazy|Recursive|BoundParameter`. Same name, different behavior. Should rename one or document the difference.
203. ~~**DRY violation (solver): inline binary_search skipping `utils::lookup_property` cache**~~ Partially done (8a8559af6) — consolidated 5 of 8 sites: `compat_overrides.rs` (2 sites), `unions.rs` (2 sites), `normalize.rs` (1 site). Remaining: `objects.rs:780` (no `ObjectShapeId` available in function signature), `unions.rs:365` (operates on mutable clone), `normalize.rs:389` (static helper without db access).
204. **DRY violation (checker): `find_property_in_object_by_str` double-wrapped** — `query_boundaries/definite_assignment.rs:38` wraps `query_boundaries/state_checking.rs:23` which wraps `tsz_solver::type_queries::find_property_in_object_by_str`. Could consolidate.
~~205. **Solver public API over-export** — ~8-10 names re-exported from `lib.rs`~~ ✅ Done (a71d1ea9e) — removed 11 unused top-level re-exports (`TypeKind`, `TypeKindVisitor`, `is_type_kind`, `RecursiveTypeCollector`, `collect_all_types`, `TypePredicateVisitor`, `TypeVisitor`, `ConstAssertionVisitor`, `ObjectTypeKind`, `classify_object_type`, `test_type`). All had zero external callers. Still accessible via `tsz_solver::visitor::*` for internal/test use. Updated `variance.rs` import path and added `use super::visitor::*` to test file.
206. ~~**DRY violation (solver): `required_param_count` / `required_element_count` inlined 6 times**~~ ✅ Done (e3a4e8826) — added `ParamInfo::is_required()` and `TupleElement::is_required()` methods on the types themselves, plus `required_param_count()` and `required_element_count()` free functions in `utils.rs`. Replaced 6 inline `.filter(|p| !p.optional && !p.rest).count()` patterns across `subtype_rules/functions.rs`, `subtype_rules/tuples.rs`, `subtype_explain.rs`, `operations/call_args.rs`, `operations/mod.rs`, `inference/infer_bct.rs`. The existing `SubtypeChecker::required_param_count` method now delegates to the shared function. One site in `contextual/extractors.rs` was left as-is (intentionally different semantics: counts `!optional` including rest params).
207. **DRY violation (checker): `get_type_from_union_type` / `get_type_from_intersection_type` / `get_type_from_type_operator` duplicated on two structs** — `types/type_node.rs` (`TypeNodeChecker`) and `types/computation/complex.rs` (`CheckerState`) have near-identical implementations. Minor differences: one calls `self.check()` vs `self.get_type_from_type_node()`, one uses `union_or_single` vs `factory.union()`.
208. **DRY violation (checker): cache-checking block copy-pasted 3× in `state/type_environment/mod.rs:1055-1110`** — Identical 10-line cache-guard + compute + insert blocks for `UNION_TYPE`, `INTERSECTION_TYPE`, and `TYPE_LITERAL`. Could extract `cached_type_node_result` helper with closure.
209. **DRY violation (checker): keyword→TypeId mapping duplicated 4+ places** — `types/type_node.rs`, `dispatch.rs` (`keyword_type_mapping`), `types/queries/lib_resolution.rs`, `tsz-lowering/src/lower/mod.rs`, `state/type_environment/mod.rs`. The `dispatch.rs` version is the most complete but others re-implement independently.
210. **DRY violation (solver): `CallSignature`→`FunctionShape` construction duplicated 4× in `subtype_rules/functions.rs:1002-1057`** — Four near-identical struct-literal constructions; only `is_constructor` field varies. Could extract `call_sig_to_fn_shape` helper.
211. **DRY violation (checker): `is_known_global_type_name` / `is_known_global_value_name` share ~30 identical entries** — `types/type_checking/declarations.rs:295` (~80 entries) and `types/queries/core.rs:477` (~70 entries). All TypedArray names + common builtins are duplicated. Could extract `const GLOBAL_BUILTINS: &[&str]`.
~~212. **Pre-existing test failure: `test_contextual_signature_instantiation_chain_no_false_ts2345`**~~ ✅ Fixed (728bb8dc5) — marked with `#[ignore]` since contextual signature instantiation chain inference is not yet supported. Was causing CI failures on every code-touching commit.
213. **DRY violation (solver): `constrain_types_impl` is ~888 lines** — `operations/constraints.rs:61-949`. Single match megablock. `Function × Function` arm (lines 401–686) is ~285 lines with rest-param-to-tuple inference logic duplicated between non-generic (lines 437–472) and generic (lines 623–656) branches. Extract `constrain_function_params_and_return` and `constrain_rest_tuple_inference` helpers.
214. **DRY violation (solver): three near-identical `constrain_*_to_*` functions** — `operations/constraints.rs:1066-1168`. `constrain_function_to_call_signature`, `constrain_call_signature_to_function`, `constrain_call_signature_to_call_signature` share identical structure (zip params, constrain this/return/predicate). Could unify via trait with `.params()`, `.return_type()`, `.type_predicate()` accessors.
215. **Magic numbers: loop bounds and capacity hints** — `import_declaration_checker.rs:220` (`0..100`), `flow/flow_analysis/usage.rs:441,468,491,508,543` (five `0..50` guards), `constraints.rs:518` (`with_capacity(28)`). Should be named constants.
216. **DRY violation (checker): `has_value`/`any_instantiated` module-value check duplicated** — `import_declaration_checker.rs:350-373` and `import_declaration_checker.rs:1391-1413` share identical "is namespace declaration instantiated → has runtime value" logic. Extract `symbol_has_runtime_value(flags, decls)`.
~~217. **Misplaced function cluster in `types/queries/core.rs`**: `check_callable_truthiness` cluster~~ ✅ Done (38dd7b4b8) — extracted TS2774/TS2872/TS2873 truthiness and callable-truthiness checking (~556 LOC) into `types/queries/callable_truthiness.rs`, reducing `core.rs` from 1,627 → 1,070 LOC.
218. **DRY violation (checker): declaration name extraction chains in other files** — `function_type.rs` and `state_checking_members/member_access.rs` contain `get_function(node).and_then(|f| arena.get(f.name)).and_then(get_identifier).map(escaped_text)` chains that could use the new `get_declaration_name_text` helper (added in 21e358d00).
219. **DRY violation (checker): `(is_async, is_generator)` extraction pattern repeated 4× in `function_type.rs`** — lines 93, 547, 766, 841. Same 3-arm conditional (`get_function → is_async/asterisk_token`, `get_method_decl → has_async_modifier/asterisk_token`, `else (false, false)`) with minor variation. Extract `fn function_async_generator_flags(&self, node_idx: NodeIndex) -> (bool, bool)`.
~~220. **Magic numbers: local `const MAX_TREE_WALK_ITERATIONS: usize = 1000` shadows global `10_000`**~~ ✅ Done (b467b92b3) — removed local shadows from `control_flow_type_guards.rs` and `enum_utils.rs` (both had 10× smaller `usize = 1000`), and redundant struct constant from `tsz-lowering/lower/mod.rs` (`usize = 10_000`). All three now use the canonical `tsz_common::limits::MAX_TREE_WALK_ITERATIONS` (`u32 = 10_000`).
221. **Magic number: `depth > 100` in `checkers/iterable_checker.rs:273`** — unnamed cycle-break guard that coincidentally matches `MAX_SUBTYPE_DEPTH`. Should use named constant or document intent.
222. **Magic number: `depth > 100` in `declarations/declarations.rs:649`** — same pattern as #221 in `is_numeric_constant_enum_expr`. Should use a named constant.
223. **Unnecessary string round-trip in `narrowing/mod.rs`** — `narrow_type` converts `TypeofKind` enum to `&str` via `.as_str()`, then `narrow_by_typeof` re-matches on the string. Both `narrow_by_typeof` and `narrow_by_typeof_negation` should accept `TypeofKind` directly. Affects `narrowing/mod.rs:1226-1233`, `narrowing/compound.rs:26-54`, `flow_analysis.rs:155`, `narrowing/utils.rs:287`, and test callers.
224. ~~**DRY violation (emitter): `param_initializer_has_top_level_await` + `first_await_default_param_name` duplicated**~~ ✅ Done — identical functions in `emitter/es5/helpers_async.rs` and `transforms/async_es5_ir.rs` consolidated into shared free functions in `transforms/emit_utils.rs`. Both struct methods now delegate to the shared implementations. Net -22 lines.
225. **DRY violation (emitter): `contains_await_recursive` near-identical** — `transforms/async_es5_ir.rs:688-1053` (~365 lines, full tree walk) and `transforms/es5.rs:1279-1345` (~66 lines, subset). Both detect `AWAIT_EXPRESSION` nodes with function-boundary guards. The `es5.rs` version is a strict subset.
226. **DRY violation (LSP): `heritage_types_contain_name` + `expression_matches_name` duplicated** — `hierarchy/type_hierarchy.rs:385-440` and `navigation/implementation.rs:264-320`. ~47 lines of identical heritage clause walking logic. Could extract to `utils.rs`.
~~227. **DRY violation (checker): `check_source_diagnostics` test helper duplicated**~~ ✅ Done (88ac493c5) — created shared `test_utils.rs` module with `check_source`, `check_source_diagnostics`, `check_js_source_diagnostics`, `check_source_codes` helpers. Replaced 7 identical parse→bind→check boilerplate copies across `binary.rs`, `type_value.rs`, `state_property_checking.rs`, `call_errors.rs`, `property_checker.rs`, `function_type.rs`, `variable_checking/core.rs`. Net -186 lines.
~~228. **DRY violation (checker): `check_and_collect` test helper duplicated in same file**~~ ✅ Done (b9b861d41) — extracted shared `test_utils` module with `pub fn check_and_collect` at file level; both `ts2481_tests` and `ts2397_tests` now import via `super::test_utils::check_and_collect`. Net -29 lines.
229. ~~**DRY violation (solver): union-member setup block repeated 4 times in `discriminants.rs`**~~ ✅ Done (493cd9ac4) — resolve type → classify union members → create PropertyAccessEvaluator 15-line block duplicated in `narrow_by_property_truthiness`, `narrow_by_discriminant`, `narrow_by_excluding_discriminant`, `narrow_by_excluding_discriminant_values`. Extracted `resolve_members_and_evaluator` helper. Net -56 lines.
230. **Divergent semantics: `is_type_reference` in solver** — `visitor_predicates.rs:168` matches `Lazy | Recursive`, `type_queries/mod.rs:170` also matches `BoundParameter`. Call sites using the visitor version silently exclude `BoundParameter`. Needs clarifying rename (like `is_type_parameter` → `is_type_parameter_like` pattern from item 73).
231. **DRY violation (emitter): `transform_expression` ~40 lines duplicated in same file** — `transforms/es5.rs`: `ES5ClassTransformer::transform_expression` (line ~739) and `ES5AsyncTransformer::transform_expression` (line ~1220) share byte-for-byte identical arms for literals, identifiers, keywords, call/property access.
~~232. **DRY violation (checker): `is_unit_type` wrapper duplicated in two query_boundaries files** — `query_boundaries/flow_analysis.rs:70` and `query_boundaries/state_checking.rs:31` had identical thin wrappers calling `tsz_solver::type_queries::is_unit_type`.~~ ✅ Done — consolidated into `query_boundaries/common.rs` with re-exports from both boundary modules.
233. **Misplaced file: `types/type_checking/property_init.rs`** — exclusively called from `state/state_checking_members/ambient_signature_checks.rs` (lines 225, 231). Should live in `state/state_checking_members/` with its callers.
234. **Misnamed file: `state/state_checking_members/class_type_param_checks.rs`** — contains TS2302 (type param refs) but also unrelated TS2512 (abstract overload consistency) and TS2516 (abstract declaration consecutiveness). Should be renamed to `class_member_modifier_checks.rs` or split further.
235. **Misnamed file: `state/state_checking_members/ambient_signature_checks.rs`** — actually contains the 4 core class member checkers (`check_property_declaration`, `check_method_declaration`, `check_constructor_declaration`, `check_accessor_declaration`), not just ambient signatures. Name is misleading historical artifact.
236. **Misplaced functions: `find_constructor_impl`, `find_method_impl`, `find_function_impl` in `types/type_checking/unused.rs`** — lines 780–906. These overload resolution helpers are called from `member_declaration_checks.rs` and `types/queries/class.rs`, unrelated to unused-declaration checking. Should move to `types/type_checking/declarations.rs`.
237. ~~**DRY violation (checker): 6 duplicated query_boundaries wrappers**~~ ✅ Done (566fed236) — `array_element_type_for_type`, `tuple_elements_for_type`, `is_type_parameter_type`, `lazy_def_id_for_type` in `call_checker.rs`, `unwrap_readonly` in `property_access.rs`, `unwrap_readonly_for_lookup` in `type_checking_utilities.rs` were identical thin-wrapper duplicates of functions in `common.rs`. Replaced with `pub(crate) use` re-exports. Net -23 lines.
238. **DRY violation (checker): `is_string_literal_type` vs `is_string_literal`** — `query_boundaries/iterable_checker.rs:46` uses `classify_for_literal_value` / `LiteralValueKind::String`; `type_queries/extended.rs:58` uses `classify_literal_type` / `LiteralTypeKind::String`. Both check for string literals but via two different solver classifier enums. The two classifier enums (`LiteralValueKind`, `LiteralTypeKind`) overlap significantly.
239. **Divergent semantics: `is_enum_type` in 3 places** — solver `visitor_extract.rs:189` (raw TypeData::Enum check), checker `checkers/enum_checker.rs:14` (symbol-flag check), checker `context/resolver.rs:419+` (union-member traversal). Three implementations answering subtly different questions about enum types.
~~240. **Misplaced functions: `is_known_*_global` in `error_reporter.rs` module gateway**~~ ✅ Done (681cd9dcf) — moved `is_known_dom_global`, `is_known_node_global`, `is_known_test_runner_global` from `error_reporter.rs` (module gateway) into `error_reporter/name_resolution.rs` where they're primarily consumed. Re-exported `is_known_dom_global` at module level for `types/computation/identifier.rs`. Gateway file reduced from 88 → 22 LOC.
241. **DRY violation (checker): `check_label_on_declaration` near-identical logic** — `declarations/declarations.rs:907-940` and `state/state_checking_members/statement_callback_bridge.rs:1130-1165` share ~33 lines of identical declaration-kind matching and error emission. Only difference: raw `ctx.error()` vs `error_at_node()`.
242. **DRY violation (checker): non-declaration statement 15-arm match** — `declarations/declarations.rs:727-745` and `state/state_checking.rs:1241-1259` share identical 15-kind `matches!()` for `STATEMENTS_ARE_NOT_ALLOWED_IN_AMBIENT_CONTEXTS`. Should extract `is_non_declaration_statement(kind)` helper.
243. **DRY violation (checker): `check_with_statement` two real implementations** — `declarations/declarations.rs:885-905` and `state/state_checking_members/statement_checks.rs:186-217`. The `declarations.rs` version is a less complete copy (missing `in_async_context()` check).
244. **DRY violation (checker): `is_external_module` logic-identical copy** — `context/strict_mode.rs:86-93` (on `CheckerContext`) and `declarations/declarations_module_helpers.rs:16-26` (on `DeclarationChecker`). Identical logic with `self.` vs `self.ctx.` prefix. `DeclarationChecker` version should delegate.
245. **Naming inconsistency: `declarations/import_checker.rs`** — the most generic name but is actually a shared helpers/utilities module (TS2307 resolution, `has_named_export_via_export_equals`), while siblings `import_declaration_checker.rs` and `import_equals_checker.rs` are the actual feature checkers. Should rename to `import_helpers.rs` or `import_common.rs`.
246. **query_boundaries micro-files**: `state_type_analysis.rs` (8 LOC, pure re-exports), `property_checker.rs` (9 LOC), `class_type.rs` (10 LOC), `dispatch.rs` (11 LOC), `generic_checker.rs` (11 LOC) — all under 15 LOC. Could consolidate `class.rs`+`class_type.rs`, `type_computation.rs`+`type_computation_access.rs`+`type_computation_complex.rs`, and merge `state_type_analysis.rs` into `state_checking.rs`.
247. ~~**DRY violation: `PropertyAccessResult::Success { type_id: TypeId::ANY, write_type: None, from_index_signature: false }` literal repeated 45+ times**~~ ✅ Done (b4e5dd9b6) — added `simple()`, `from_index()`, `with_write_type()` convenience constructors on `PropertyAccessResult`. Replaced ~45 identical 4-line struct literals across `property.rs`, `property_helpers.rs`, `property_visitor.rs`. Net -131 lines.
248. **DRY violation (solver): bounded `for _ in 0..8` Lazy+eval normalize loop** — identical in `operations/core.rs:242-260` (`normalize_union_member`) and `relations/compat.rs:232-253` (`normalize_assignability_operand`), plus 2 more instances in `operations/generic_call.rs:600-606,1016-1022`. Extract `normalize_type_lazy_and_eval()` free function.
249. **DRY violation (solver): evaluate-then-try-object-member-then-ANY pattern** — `property.rs` lines 682-721, 725-747, 750-783, 786-817 share identical 15-line skeleton (evaluate → recurse if changed → object member → ANY fallback). Extract `resolve_via_evaluation()` private helper.
250. **DRY violation (solver): binary op preamble in `binary_ops.rs`** — `evaluate_plus:596`, `evaluate_arithmetic:665`, `evaluate_comparison:707` share identical 17-line UNKNOWN/ERROR/symbol/ANY guard sequence. Extract `normalize_binary_operands()` helper.
251. **DRY violation (solver): union filter → NEVER/single/new union idiom** — repeated ~5 times in `narrowing/compound.rs` and `narrowing/core.rs`. Existing `union_or_single_preserve` helper is missing the `len == members.len() → return original` identity branch. Extend it to accept an `original_type: TypeId` parameter.
252. **DRY violation (checker): `symbol.primary_decl()` pattern** — 20+ sites repeat `if symbol.value_declaration.is_some() { value_declaration } else { declarations.first().copied().unwrap_or(NONE) }`. Add `Symbol::primary_declaration() -> NodeIndex` helper on binder `Symbol`.
253. **DRY violation (checker): manual `lib_binders` Vec construction** — `declarations/import/core.rs:74-79,552-557` manually maps `lib_contexts` to binder Arcs instead of calling existing `self.get_lib_binders()`.
254. **DRY violation (checker): namespace export merge loop** — `declarations/namespace_checker.rs` lines 76-120 and 199-242 are copy-pasted verbatim. Extract `merge_exports_into_props()` helper.
