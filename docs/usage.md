```markdown
# Usage

The `hello_world` function is a simple utility that returns a greeting string.

## Installation

To use this function, ensure the package is included in your project dependencies.

## Example

Here is how to call the function:

```python
from my_package import hello_world

greeting = hello_world()
print(greeting)
```

**Output:**

```
Hello, World!
```
