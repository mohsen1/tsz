# [WIP] fix(checker): suppress false TS2345 in contextual callback unions

- **Date**: 2026-05-06
- **Branch**: `fix/conformance-20260506-200422`
- **PR**: https://github.com/mohsen1/tsz/pull/4294
- **Status**: abandoned
- **Workstream**: 1 (Diagnostic conformance)

## Intent

Fix the picked false-positive conformance failure
`TypeScript/tests/cases/compiler/inferenceContextualReturnTypeUnion1.ts`.
`tsc` accepts the file, while `tsz` currently emits an extra `TS2345` around
generic callback inference through optional and union contextual callback
types. This slice will root-cause the contextual typing/inference boundary and
fix the owning checker or solver path rather than suppressing the fixture.

Abandoned before implementation: on current `origin/main` (`1162930bb1`),
the targeted verbose conformance run already passes 1/1 with no
fingerprint-only failure, so the picker input was stale and there is no
failure left for this slice to fix.

## Files Touched

- TBD

## Verification

- `./scripts/session/quick-pick.sh` selected
  `TypeScript/tests/cases/compiler/inferenceContextualReturnTypeUnion1.ts`.
- `./scripts/conformance/conformance.sh run --filter "inferenceContextualReturnTypeUnion1" --verbose` — 1/1 passed; no failure remains on `origin/main`.
