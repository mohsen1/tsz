# [WIP] fix(checker): align Temporal conformance diagnostics

- **Date**: 2026-05-06
- **Branch**: `fix/conformance-20260506-202103`
- **PR**: https://github.com/mohsen1/tsz/pull/4297
- **Status**: claim
- **Workstream**: 1 (Diagnostic conformance)

## Intent

Fix the verified conformance failure
`TypeScript/tests/cases/compiler/temporal.ts`. The current filtered run expects
`TS2339` and `TS2552`, while `tsz` emits `TS2339` plus extra `TS2345`
assignability errors for Temporal option objects. This slice will root-cause
the remaining Temporal lib diagnostic gap and option-object assignability path
without adding fixture suppressions.

## Files Touched

- TBD

## Verification

- `./scripts/session/quick-pick.sh --run` selected and reproduced
  `TypeScript/tests/cases/compiler/temporal.ts`.
