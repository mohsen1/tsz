# fix(checker): resolve checked-JS require aliases in JSDoc type positions

- **Date**: 2026-05-06
- **Branch**: `fix/conformance-20260506-201456`
- **PR**: TBD
- **Status**: claim
- **Workstream**: 1 (Diagnostic conformance)

## Intent

Fix the picked conformance failure
`TypeScript/tests/cases/conformance/salsa/varRequireFromJavascript.ts`.
tsz currently emits an extra `TS2552` for a checked-JS `var ex =
require("./ex")` alias used in a JSDoc type reference, while tsc accepts the
same alias for both value and type positions.

## Files Touched

- TBD

## Verification

- `./scripts/session/quick-pick.sh` selected
  `TypeScript/tests/cases/conformance/salsa/varRequireFromJavascript.ts`.
