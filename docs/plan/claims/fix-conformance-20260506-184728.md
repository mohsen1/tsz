# fix(checker): avoid extra TS1210 in class static blocks

- **Date**: 2026-05-06
- **Branch**: `fix/conformance-20260506-184728`
- **PR**: TBD
- **Status**: claim
- **Workstream**: 1 (Diagnostic conformance)

## Intent

Fix the picked conformance failure
`TypeScript/tests/cases/conformance/classes/classStaticBlock/classStaticBlock6.ts`.
tsz currently emits the expected `TS1109` plus an extra `TS1210`; tsc only
expects `TS1109` for this fixture. The slice will identify why the static-block
body is producing the additional strict-mode identifier diagnostic and fix the
owning parser/checker path rather than suppressing the conformance output.

## Files Touched

- TBD

## Verification

- `./scripts/session/quick-pick.sh` selected
  `TypeScript/tests/cases/conformance/classes/classStaticBlock/classStaticBlock6.ts`.
