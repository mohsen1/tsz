# Known-failures baseline for scripts/ci/known-failures-check.mjs (#15399).
# One `binary-id::test-name` per line; blank lines and `#` comments ignored.
#
# Regenerate from a full signoff run (fail-fast disabled, junit on):
#   cargo nextest run --profile signoff --workspace || true
#   node scripts/ci/known-failures-check.mjs --update
#
# Until the first `--update` there is no reconciled marker and the checker
# runs in advisory mode (never blocks). `--update` stamps the marker and
# switches the checker to strict: any failing test not listed here is a new
# regression, even when the list is empty (a fully green tree). In normal PRs
# this list may only shrink; a deliberate re-reconcile must bump the marker's
# `r<N>` generation in the same diff (`--update --bump-generation`), which is
# what scripts/ci/check-known-failures-growth.py accepts as authorization.
# 2026-07-14 re-reconcile (r3): five rows surfaced once the unit job stopped
# dying at its first failing suite. Adjudicated in the goal campaign
# (scratchpad adj-* reports; tasks remain open for the two real gaps):
# - issue_14123 / kysely / js_export_surface: HARNESS-ARTIFACTS — the real
#   binary matches the tsc 7.0.2 oracle on the exact fixtures; the in-process
#   harness diverges (full-lib parse cost + TS2589 fuel, cross-file resolution
#   fidelity, two-binder IdentText identity).
# - js_constructor_property: TS7 half-migration — Ctor.prototype = { m() {} }
#   methods must contextually type `this` as the object literal (oracle emits
#   TS2339s; tsz emits neither old TS2322 nor new TS2339s).
# 2026-07-17 shrink (r4, no generation bump — removals are always allowed):
# two rows now pass on main @ 5aa7e0b4e3 and were dropped. Evidence: CI-exact
# `unit-nextest.sh --gate --packages tsz-checker` (10170 tests, 25 failing, 0
# new; the js_constructor row reported as the lone shrink) plus a targeted
# `cargo nextest -p tsz-cli --lib` (the cli row ran+passed). Both flipped via
# today's cross-file class-value import-cycle / prototype-object landings
# (tasks #2, #4):
# - js_constructor_property_more_tests::test_generic_constructor_prototype_object_methods_keep_existing_member_checks
#   (js_constructor_property_more_tests binary: 45/45 pass)
# - tsz-cli ...cross_file_class_namespace_merge_value_keeps_call_signature_in_import_cycle
# 2026-07-19 re-reconcile (r6): one ADDITION with the revert of 0e4409ffc8
# (task #83 adjudication). The cross-file class-value import-cycle deferral
# fixed this test but regressed neverthrow with 3 canary FPs (plain classes in
# barrel cycles lose statics/construct sigs — the companion is built once,
# mid-cycle, incomplete; no post-cycle rebuild exists). Deferral-off is
# byte-identical on all other canary rows, so the deferral's only wins were its
# own tests. The test returns to its pre-0e4409ffc8 tracked-failing state
# pending the architectural pure-instance-DefId item (task #2); full saga:
# session ledger m4-snapshot-defid + neverthrow-fixforward reports.
# 2026-07-28 re-reconcile (r7): four ADDITIONS adjudicated as pre-existing on
# clean origin/main @ 6e802914bf (0/4 pass with per-target runs) — drift from
# #15846/#15965 via the per-merge lane; tracked in issue #15969 for fix-or-
# remove. Also the 2026-07-27 block (24 additions) rode r6's format.
# Known-flaky cross-file/cross-arena non-determinism in the PARALLEL checker:
# `check_files_parallel` intermittently emits a spurious TS2339/2345/2322 for a
# zod-style cross-file IssueData spread depending on inter-file check ordering
# (fails deterministically in some local envs, intermittent in CI). It is the
# sole remaining blocker stalling the whole merge queue. Baselined to unblock;
# the shrink-ratchet will flag it here once the cross-arena identity fix lands.
# 24 entries adjudicated 2026-07-27: verified failing on clean origin/main
# (b49ac71eb8) with per-target runs; accumulated via per-merge CI (clippy +
# arch-size only). See d6224a58d3 for the attribution method.
# 2026-07-28: #15846 reverted (496893ec21) — its 3 symbol-keyed entries removed;
# #15967's TS7 oracle migration fixed #15965's js_constructor row (shrink).
# 2026-07-28 re-reconcile (r8): eight tsz-cli ADDITIONS, each verified failing on
# clean origin/main — pre-existing cli-lane drift first surfaced when a push
# touched tsz-cli (earlier inventories covered checker/emitter/solver only).
# 2026-07-29 re-reconcile (r9): twelve ADDITIONS, each verified failing at
# detached origin/main @ ff739449ec with per-target runs — drift from the
# 2026-07-28 codex batch, whose per-push gates ran SCOPED suites (the full
# 3-crate matrix first ran on the next checker+cli+core push). Three of the
# twelve (mapped_filter non_distributive + intersection const_assignment/
# member_read) are ORDER-DEPENDENT within their suites: they passed one full
# run and failed the next, on identical trees — same cross-arena
# non-determinism class as the zod parallel flake above. Fix-or-remove in
# issue #15983.
# 2026-07-31 re-reconcile (r10): two ADDITIONS, both verified failing on clean
# origin/main @ 28d4fcb (per-target run: `cargo test -p tsz-checker --test
# generic_call_assignment_flow_tests`, 25 passed / 2 failed). Neither is a
# harness ordering artifact discovered by this session — both were already
# root-caused by earlier ones:
# - colliding_generic_terminals_keep_flow_diagnostics_clean_across_file_orders
#   deliberately constructs two files whose exported terminals collide on a
#   raw binder-relative SymbolId (the test asserts the collision as a
#   precondition) and is the same contested-cross-file-identity class as
#   #15983/#14344 — false TS18048s once the flow-fallback path resolves the
#   wrong file's terminal through the colliding id.
# - imported_generic_result_narrows_inside_reduce_arrow was traced (not by
#   this session) to a different mechanism: the callee's deferred generic
#   result resolves to a still-unresolved `TypeData::Lazy(DefId)` at
#   flow-fallback time, and `flow_call_fallback.rs::single_non_rest_generic_call`
#   has no arm to force-resolve it, falling into the `Unsupported` catch-all —
#   reframed as a slice of #15396/#13979, not the #15983 SymbolId family.
# Fix-or-remove tracked in #15983 and #15396/#13979 respectively; #15999
# closed as superseded (its other three sections were independently fixed by
# #16002/#16003/#16006/#16007).
# 2026-08-06 shrink (no generation bump — removals are always allowed): the two
# kysely_callback_context_tests conditional-builder rows now pass. They were the
# #15983 SymbolId-collision family surfacing through the lib-aware multi-file
# harness: base-0 per-file binders aliased distinct-but-same-named module
# exports (e.g. two files' `ResultBuilder`) on one raw SymbolId, which the
# dynamic cross-file overlay could not disambiguate. Fixed by migrating the two
# rows to the new production-faithful `check_multi_file_with_libs_unique_module_locals`
# helper, which gives each file's module-local suffix a disjoint id range while
# keeping the shared lib prefix's ids (mirroring the driver's bind-result reducer).
# baseline-status: reconciled r10

# 2026-08-06 shrink (no generation bump — removals are always allowed): six
# rows now pass on main @ 0001e7a725, verified with per-target runs
# (`cargo nextest run -p <pkg> --test <target>`), not a scoped filter. Three
# alias-intersection rows and the reexported-symbol row are #15983 entries that
# the cross-file identity landings have since fixed; the ts2303 self-import row
# and the tsz-emitter overloaded-initializer row likewise pass. Full per-target
# sweep of every target holding a gate entry: 30 failing, all remaining entries
# still reproduce.
# 2026-08-11 shrink (no generation bump — removals are always allowed): three
# more #15983-family rows now pass, verified with per-target runs
# (`cargo test -p tsz-checker --test <target>`). All three shared one root
# cause: `import_type_ambient_module_member_tests::cross_file_import_type_member_still_works`
# and two `import_type_utility_identity_tests` rows built their two-file
# fixture with the plain `check_multi_file_with_libs` helper, whose per-file
# binders both restart `SymbolId` at 0. An unrelated declaration in one file
# (e.g. `Member`) landed on the same raw id as a declaration in the other file
# (e.g. `Thing`), and `CheckerState::get_or_create_def_id`/`get_or_create_def_id_for_symbol_name`
# — keyed by that raw id alone — collapsed them onto one `DefId`, corrupting
# cross-file identity (a spurious TS2456 self-cycle in one case, an `infer`
# binding or conditional check resolving to the wrong declaration in the
# other two). Fixed by migrating both test files to
# `check_multi_file_with_libs_unique_module_locals`, same as the 2026-08-06
# kysely fix above. `import_type_extends_structural_match_no_false_positive`
# (#17158) is now fixed and delisted: a bare `import("mod").X` type argument is
# re-resolved through the checker's canonical import path instead of keeping the
# `TypeLowering` cross-arena re-mint, so it shares identity with a plain `X`
# reference and the conditional branch selects correctly.
# 2026-08-13 shrink (no generation bump — removals are always allowed): the two
# jsdoc_constructor_typeof_source_display rows now pass and were rewritten to
# match the pinned typescript@7.0.2 oracle. Root cause: a Closure `function(...)`
# JSDoc type was rejected with TS1005 (correct) but then dropped to `any` instead
# of resolving to the global `Function` type the way tsc does after that
# recovery. `resolve_jsdoc_reference` now maps any Closure `function(...)` head to
# `Function`, so `@param`/`@type`/`@return` positions participate in downstream
# assignability (a non-function argument is a TS2345/TS2322 against `Function`);
# the arrow form `(...) => T` still parses structurally and is unaffected. The
# two rows' original TS2345 expectation was never reachable under TS7 (a function
# value is assignable to `Function`, so there is no TS2345), so they were renamed
# and re-pinned to the oracle-confirmed TS1005 + `Function` behavior.
# 2026-08-18 shrink (no generation bump — removals are always allowed):
# `conditional_application_display_tests::deferred_generic_conditional_keeps_branch_union`
# now passes — fixed, not incidental. TS2322's source-display formatter kept a
# still-generic deferred conditional's alias spelling (`F<T>`) unconditionally
# whenever the source still carried a free type parameter, with no check of
# whether the paired TARGET was itself generic/deferred. tsc only keeps that
# spelling when the target is ALSO generic (already handled by
# `generic_deferred_source_keeps_spelling_against_generic_target`); against a
# concrete target it expands the source to its branch union (`string |
# boolean`). Added the mirror guard
# `deferred_conditional_source_branch_union_display`
# (`error_reporter/conditional_alias_display.rs`), wired into
# `format_assignment_source_type_for_diagnostic`
# (`error_reporter/core/diagnostic_source/assignment_formatting.rs`) right
# after the existing guard. Reuses the checker's existing determinism/
# ambiguity logic rather than duplicating it: `compute_ambiguous_conditional_display`
# (`error_reporter/core_alias_display.rs`) was refactored into
# `compute_ambiguous_conditional_display_inner(ty, keep_unconstrained_named_alias)`,
# and the new guard calls it with the carve-out disabled. A first version that
# hand-rolled the union directly regressed
# `distributiveConditionalTypeConstraints.ts` (f2/f3/f4 in the TS conformance
# corpus): it ignored that the check-type parameter's own declared constraint
# can already decide a single branch deterministically (`T extends unknown[]`
# in `IsArray<T> = T extends unknown[] ? true : false` always selects `true`),
# a case tsc keeps the alias for and elaborates against just that branch —
# caught by `compare-to-parent.sh` (1 newly failing) before landing, fixed by
# the refactor/reuse instead. Bails to the pre-existing alias-keeping behavior
# whenever a branch is the bare check-type-parameter itself (`Extract<T, U> =
# T extends U ? T : never`) or wraps rather than is the check parameter (`T
# extends number ? T[] : boolean` — a known, deliberately out-of-scope gap;
# tsc partially expands such branches, tsz keeps the full alias). Verified:
# direct per-target run (`cargo test -p tsz-checker --test
# conditional_application_display_tests`), 13/13 passed (was 9/10, with 4 new
# adjacent-case tests); `cargo clippy --workspace --all-targets` clean;
# `compare-to-parent.sh origin/main` 0 newly-failing / 0 newly-passing
# (11659/11659 both sides — this row isn't part of the TS conformance corpus).
# 2026-08-18 shrink (no generation bump — removals are always allowed): the
# fp_repro_audit_2026::issue_14944_genuine_widening_self_assignment_still_errors
# row now passes — fixed, not incidental. The killing-definition reduction base
# in `flow_traversal.rs` only recovered a declared union from
# `VARIABLE_DECLARATION` annotations; for an annotated *parameter* binding it
# degraded to the loop-narrowed `initial_type`, so a widening back-edge write
# (`x = x.length` / `x = n` with `x: string | number` narrowed to `string`)
# could never re-widen the loop-head join and the genuine TS2339 was dropped.
# Now falls back to `fallback_declared_annotation_type`, which reads parameter
# annotations syntactically. Verified with a direct per-target run
# (`cargo test -p tsz-checker --test conformance_issues_types issue_14944`,
# 7/7 passed including four new adjacent-matrix tests and a no-error negative
# control), plus oracle runs on all variants.
# 2026-08-18 shrink (no generation bump — removals are always allowed): the two
# conformance_issues_types::types::r#enum rows
# (test_jsdoc_object_literal_property_types_do_not_trigger_self_tdz,
# test_jsdoc_object_literal_shorthand_and_default_param_preserve_source_types)
# and the js_jsdoc_diagnostics_tests row
# (checked_js_async_jsdoc_expression_body_ts2322_anchors_on_return_expr) were
# stale pre-TS7 expectations, not compiler bugs. All three fixtures annotate
# with the Closure `@type {function(...): T}` spelling, which TypeScript 7
# rejects outright with TS1005 `'}' expected.` (plus TS8030 for method-syntax
# properties) and gives no type — so the contextual typing those tests
# asserted (TS2322 drilled into an async arrow's expression body, TS2322 on a
# default parameter initializer, TS2345 at a call site) can never fire.
# Oracle-confirmed against `typescript@7.0.2` on all three fixtures; tsz's
# actual output already matched the TS7 oracle exactly, including per-code
# counts (TS2322 x2 / TS1005 x3 / TS8030 x1 / TS7006 x2 on the object-literal
# fixture). Renamed/re-pinned the tests to the TS7 oracle — same fix shape as
# the 2026-08-13 entry and the ts7014 re-pin below. No production code
# changed. Verified: 3/3 re-pinned tests pass, plus full per-target runs of
# both affected binaries.
# 2026-08-18 shrink (no generation bump — removals are always allowed): the
# imported_generic_result_narrows_inside_reduce_arrow row now passes — fixed,
# not incidental. The 2026-07-31 note below blamed a missing `TypeData::Lazy`
# arm in `single_non_rest_generic_call`; a later session refuted that (adding
# the arm resolves nothing — the cross-file `DefId` was never registered in
# any `TypeEnvironment`). Actual mechanism: the read-only flow syntactic
# fallback runs during return-type inference, which never checks sibling
# assignment statements, so an imported generic callee's alias target has no
# computed type anywhere at that moment — unavoidable in import cycles, where
# no file check order can type the provider first. Fix: the fallback records
# the unresolved import-alias callee in
# `flow_shared.unresolved_import_callees`; `check_flow_usage` (the `&mut`
# boundary) drains it, forces `get_type_of_symbol` on the alias, drops the
# flow results computed while the callee was unresolved, and re-runs the
# narrowing once. Oracle-confirmed (typescript@7.0.2 and 6.0.2 both clean on
# the fixture; tsz previously emitted a false TS18048). Verified with a
# per-target run: `cargo test -p tsz-checker --test
# generic_call_assignment_flow_tests` — 29 passed (was 27/29 with 2 failing),
# including three new adjacent-matrix tests (renamed binders, import-cycle
# form, nullable-result negative control pinning TS2345+TS18048). The
# colliding_generic_terminals row below is the separate #15983
# SymbolId-collision family and still fails.
tsz-checker::generic_call_assignment_flow_tests::colliding_generic_terminals_keep_flow_diagnostics_clean_across_file_orders
tsz-checker::generic_generator_member_substitution_tests::generic_generator_genuine_mismatch_still_errors_with_substituted_source
# 2026-08-18 shrink (no generation bump — removals are always allowed): the
# issue_14123_repro_tests::issue_14123_alias_array_infer_reports_mismatch row
# is delisted by fixing the HARNESS-ARTIFACT the r3 adjudication named (top of
# this file): the suite's recursion unwraps through a lib generic (`Promise`),
# and the plain `check_source_with_libs` harness cannot unify a lib generic's
# base declaration across the user and lib arenas (#16125), so the alias
# application stayed an unevaluated self-application in relation position and
# the mismatch fixture lost its TS2322 (every target except `never` was
# silently accepted). The suite now routes through the production-faithful
# `check_source_with_libs_shared_def_store` construction; the previously
# failing fixture reports exactly one TS2322 whose source side renders the
# converged `{ id: 0; }` fixpoint, oracle-exact. No production code changed.
# Verified with a per-target run: `cargo test -p tsz-checker --test
# issue_14123_repro_tests` — 8/8 passed (was 6/7; +1 new primitive-target
# adjacent witness, also oracle-verified), including both TS2589 safety nets.
# 2026-08-18 shrink (no generation bump — removals are always allowed): the two
# js_export_surface_tests::part_01 rows below now pass — verified with a direct
# per-target run (`cargo test -p tsz-checker --test js_export_surface_tests`),
# 81/81 passed (was 79/81). Fixed incidentally by #17610's CommonJS
# export-property ordering fix: the `current_file_commonjs_named_export_type`
# fast path in `types/property_access_type/resolve.rs` always resolved a
# read to the last assignment in the *whole file*, even for a property #17610
# made "ordered" (one with a non-aliasable direct assignment) — an ordered
# property's read must use the last assignment textually *before* the read
# instead. Routed ordered reads through the already-existing position-bounded
# `current_file_commonjs_prior_named_export_type` helper.
tsz-checker::raw_builder_return_context_inference_tests::different_base_and_ambiguous_union_do_not_pin_the_tag
# 2026-08-17 shrink (no generation bump — removals are always allowed): the two
# tsz-cli::driver_tests rows below now pass on main @ 026b8458 — verified with a
# direct per-test run (`cargo nextest run -p tsz-cli --lib -E
# 'test(cross_file_dependent_operand_aliases_accept_scalars_and_literal_arrays) or
# test(declaration_emit_reports_ts7056_for_private_import_type_alias)'`), 2/2
# passed. #17551 (the untracked-failures issue that first surfaced this
# baseline's tsz-cli rows) named the TS7056 row as a real missing-diagnostic
# bug with a conformance twin (`declarationEmitPrivatePromiseLikeInterface.ts`)
# and asked for the other five to be checked against the oracle before being
# treated as compiler bugs rather than pinning bugs; this shrink is that check
# for two of the five — both simply pass now, most likely fixed incidentally by
# intervening cross-file/instantiation-identity landings between #17551's
# 2026-08-15 measurement and today. The two other tsz-cli rows #17551 surfaced
# (`cross_file_class_namespace_merge_value_keeps_call_signature_in_import_cycle`
# and `default_lib_validation_normalizes_cross_arena_method_members_after_global_merge`)
# have both since been fixed and removed — see the two shrink notes below.
#
# 2026-08-18 shrink (no generation bump — removals are always allowed):
# `cross_file_class_namespace_merge_value_keeps_call_signature_in_import_cycle`
# (the live #17567 investigation) now passes — fixed by teaching
# `is_incomplete_class_type`'s `Lazy(DefId)` arm to check
# `class_instance_resolution_set` before evaluating the identity away, and
# wiring it into `validate_type_args_against_params`'s constraint-satisfied
# check as a deferred-like-tsc disjunct (#17619).
#
# 2026-08-18 shrink (no generation bump — removals are always allowed):
# `default_lib_validation_normalizes_cross_arena_method_members_after_global_merge`
# now passes (#17595). Its default-lib TS2430 (`Document` incorrectly extends
# `NonElementParentNode`) was a heritage-thin `HTMLElement` — momentarily
# resolved without `Node`'s inherited members during the DOM
# `Node`/`Element`/`HTMLElement` resolution cycle — being cached over the
# complete body via `resolve_lib_type_by_name_inner`'s direct `symbol_types`
# write, which bypassed the membership-monotone guard the finalize path already
# applies. Fixed by extending that guard to the direct cache write.
# 2026-08-18 shrink (no generation bump — removals are always allowed): the two
# conformance_issues_features::namespace_construct_signature rows below now pass
# — verified with a direct per-target run (`cargo test -p tsz-checker --test
# conformance_issues_features`), 388/388 passed (was 388/389, one failure). The
# one live failure in that run was a THIRD, untracked test in the same file
# (`part_00::test_jsdoc_param_function_type_without_return_reports_ts7014`,
# never in this baseline — pre-existing drift, not caused by this session):
# it asserted the pre-TS7 TS7014 ("lacks return-type annotation") diagnostic
# for a Closure-style `function(...)` JSDoc type. TypeScript 7 rejects that
# construct outright with TS1005 before any function-type node is built, so
# TS7014 can never fire for it — the same landed fix this baseline's
# 2026-08-13 entry describes (`resolve_jsdoc_reference` mapping Closure
# `function(...)` heads to `Function`), just never back-ported to this one
# test. Renamed/re-pinned it to assert TS1005 present and TS7014 absent,
# matching the oracle-pinned sibling assertions already in
# `jsdoc_constructor_typeof_source_display_tests.rs`
# (`jsdoc_closure_function_param_missing_return_is_not_ts7014` et al). No
# production code changed. The other rows in the batch this session sampled
# (`test_module_exports_forward_read_reports_ts2565`,
# `test_plain_js_object_expando_write_stays_open_world_in_check_js` in
# `js_export_surface_tests`, both already/still tracked or untracked
# elsewhere) are genuine, oracle-confirmed missing-diagnostic bugs (TS2565,
# TS2300) independent of any TS2318 no-lib noise in their fixtures — not
# re-attempted this session, worth their own issue.
# 2026-08-17 shrink (no generation bump — removals are always allowed): the four
# tsz-checker::cross_module_nested_interface_tests rows now pass — HARNESS-ARTIFACTS,
# not a compiler bug. `compile_module_files`/`compile_two_module_files` used the
# plain `check_multi_file` helper, which gives every file's binder a base-0
# `SymbolId` arena (see `PER_FILE_SYMBOL_BASE_STRIDE`'s doc comment in
# `test_utils/multi_file.rs`); a 3-file chain's `commands.ts` import-equals
# binding and `server.ts`'s exported interface landed on colliding raw ids, so
# `config.workspace.name` resolved through the wrong file's identically
# numbered symbol and reported "Property 'name' does not exist on type
# 'server'" instead of checking `IWorkspace`. Confirmed as a pure harness
# artifact: a real `tsz` CLI binary run on the equivalent 2-file repro already
# matched the `tsc` 6.0.2 oracle exactly (`TS2345`) before this fix. Migrated
# both helpers to `check_multi_file_with_libs_unique_module_locals` (same
# #15983-family fix already applied to the kysely/import-type rows above),
# which gives each file's module-local symbols a disjoint id range even with
# no libs loaded. Verified with a direct per-target run: `cargo test -p
# tsz-checker --test cross_module_nested_interface_tests`, 22/22 passed (was
# 18/22).
