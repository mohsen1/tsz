You are working in one of the ~/code/tsz-* repositories. The number in your directory name (e.g., tsz-3) out of the total number of available tsz-* directories dictates your chunk (e.g., 3 out of 12). Calculate your chunk's `--offset` and `--max` (assuming ~12584 total tests) and use them in your `scripts/conformance.sh` runs to focus exclusively on your partition.
Goal: improve TypeScript conformance parity with small, safe, incremental commits.

Hard requirements for this run:
1) Verify hooks are active. If `git config --get core.hooksPath` is not `scripts/githooks`, run `./scripts/setup.sh --quick`.
2) Capture BEFORE baseline and record exact results:
   - `cargo nextest run --workspace`
   - `./scripts/conformance.sh run`
   Save pass/fail totals and conformance pass percentage.
3) Pick one high-signal failing conformance case.
4) Implement a minimal targeted fix.
5) Run targeted conformance for that case.
6) Run a broader safety slice (using your chunk's --offset and --max) to catch regressions.
7) Run CI lint parity commands before commit:
   - `cargo clippy -p tsz-common -p tsz-scanner -p tsz-parser -p tsz-binder -p tsz-solver -p tsz-checker -p tsz-emitter -p tsz-lowering -p tsz-lsp --all-targets -- -D warnings`
   - `cargo clippy --all-targets --all-features`
8) Capture AFTER baseline and compare to BEFORE:
   - `cargo nextest run --workspace`
   - `./scripts/conformance.sh run`
   Do not commit if tests regress or conformance percentage drops.
9) If improved or fixed without regression, create ONE small commit.
10) Sync with main using: git pull --rebase origin main, then git push origin main.
11) Do not ask user questions. Keep going until this run is complete.
