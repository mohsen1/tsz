{
  "campaign": "big3-unification",
  "baseline_at_creation": 11745,
  "current_pass": 11754,
  "current_big3_wrong_code": 68,
  "status": "active",
  "sessions": [
    {
      "date": "2026-04-06",
      "summary": "Fixed globalThis property resolution for Window & typeof globalThis intersection types",
      "invariant": "When resolving properties on intersection types like Window & typeof globalThis, tsz should check properties from ALL intersection members (Window interface first), not just shortcut to globalThis property resolution",
      "tests_fixed": [
        "globalMergeWithCommonJSAssignmentDeclaration.ts"
      ],
      "kpi_impact": "+2 conformance tests passing, overall 11747/12581 (93.4%)",
      "changes": [
        "crates/tsz-checker/src/state/type_environment/type_node_resolution.rs"
      ],
      "regressions": "None detected in 200-test regression check"
    },
    {
      "date": "2026-04-06",
      "summary": "Fixed parameter type extraction from overloaded generic callables",
      "invariant": "When extracting parameter types for contextual typing from callable types with multiple generic signatures, tsz should collect parameter types from ALL signatures and return the common type if they agree, rather than returning None early",
      "tests_fixed": [
        "2 tests in big3/contextual-typing campaigns"
      ],
      "kpi_impact": "Big3 wrong-code reduced from 71 to 69 (2 tests improved), overall 11744/12581 (93.3%)",
      "changes": [
        "crates/tsz-solver/src/contextual/extractors.rs"
      ],
      "regressions": "None detected in 100-test regression check"
    },
    {
      "date": "2026-04-06",
      "summary": "Added support for strictBuiltinIteratorReturn compiler option in conformance runner",
      "invariant": "The conformance test runner must properly pass the strictBuiltinIteratorReturn option to enable iterator return type checking for tests that use this directive",
      "tests_fixed": [
        "contextualTypingWithGenericAndNonGenericSignature.ts",
        "jsxRuntimePragma.ts",
        "checkJsxChildrenProperty2.tsx"
      ],
      "kpi_impact": "+3 tests passing (11745 -> 11748), Big3 wrong-code count at 68",
      "changes": [
        "crates/conformance/src/options_convert.rs",
        "crates/tsz-cli/src/bin/tsz_server/main.rs",
        "crates/tsz-cli/src/bin/tsz_server/check.rs"
      ],
      "regressions": "None detected in full conformance suite"
    },
    {
      "date": "2026-04-06",
      "summary": "Fixed TS2608 error position for JSX.ElementAttributesProperty",
      "invariant": "When JSX.ElementAttributesProperty has more than one property, TS2608 should be emitted on the interface declaration (the interface name identifier) rather than on JSX element usages",
      "tests_fixed": [
        "tsxElementResolution15.tsx"
      ],
      "kpi_impact": "+1 test passing, overall 11746/12581 (93.4%)",
      "changes": [
        "crates/tsz-checker/src/checkers/jsx/extraction.rs"
      ],
      "regressions": "None detected in full conformance suite"
    },
    {
      "date": "2026-04-06",
      "summary": "Added helper for assignability errors with failure reason",
      "invariant": "Assignability error reporting should use a centralized helper to render failure reasons with display types for consistent error messages",
      "tests_fixed": [],
      "kpi_impact": "No test changes - code refactoring only",
      "changes": [
        "crates/tsz-checker/src/assignability/assignability_diagnostics.rs",
        "crates/tsz-checker/src/error_reporter/assignability_helpers.rs"
      ],
      "regressions": "None detected in full conformance suite"
    },
    {
      "date": "2026-04-06",
      "summary": "Fixed callback return type error display to show full function types instead of just return types",
      "invariant": "When reporting callback return type mismatches (TS2322), tsz should display the full function type (e.g., '() => string') instead of just the return type (e.g., 'string'), matching tsc behavior",
      "tests_fixed": [
        "contextualTyping33.ts"
      ],
      "kpi_impact": "+1 test passing (11753 -> 11754), Big3 maintained at 68, 3138 checker tests passing",
      "changes": [
        "crates/tsz-checker/src/error_reporter/call_errors/elaboration.rs",
        "crates/tsz-checker/src/assignability/assignability_diagnostics.rs",
        "crates/tsz-checker/src/error_reporter/assignability_helpers.rs",
        "crates/tsz-checker/src/error_reporter/render_failure.rs"
      ],
      "regressions": "None detected in full conformance suite"
    }
  ],
  "known_dead_ends": [
    "Analyzed 4 representative tests: chainedPrototypeAssignment (require resolution), genericDefaults (default type params), strictSubtypeAndNarrowing (type predicates), assignmentToVoidZero1 (flow analysis)",
    "2026-04-13: cross-file interface merging (mergedInterfaceFromMultipleFiles1.ts, parameterNamesInTypeParameterList.ts, typeOfThisInInstanceMember2.ts). Attempted single-session fix via per-declaration arena delegation in compute_interface_type_from_declarations (added helper compute_interface_decl_type_in_arena in cross_file.rs). Reverted because the root cause is deeper: per-file binders only see their own file's declarations for the symbol (decls_count=1 in debug log, when tsc sees 2). The symbol-level merge happens in tsz_core::parallel::core.rs only for non-external-module files, but the symbol ID visible to the checker in _1.ts doesn't resolve to the globally-merged symbol — it remains the per-file local view. The ///<reference path=> tests need either: (a) checker uses the GLOBAL merged symbol (via merged_symbols map in parallel merge), OR (b) per-file binders each expose all declarations via cross-file lookup. Touching this requires coordinated changes across tsz-binder, tsz-core/parallel, and checker symbol resolution — beyond a single agent cycle."
  ],
  "promising_leads": [
    "assignmentToVoidZero1.ts is close-to-passing (diff=1) with missing TS2322 - flow analysis issue",
    "genericDefaults.ts has extra TS2345 from generic default handling",
    "2026-04-17 (RESOLVED — NOT BIG3): genericFunctionInference1.ts line 160 'Type string[] is not assignable to type string[]'. Instrumented execute_relation: source = Array(TypeId(3)=UNKNOWN), target = Array(TypeId(10)=STRING). The relation correctly fails; UNKNOWN ≠ STRING. Root cause: missing higher-order function inference (TS 3.4) — when calling `map(identity)` with `identity: <T>(value: T) => T`, tsc carries the inner type variable into the outer (T_map = T_identity), producing a still-generic result `<T>(arr: T[]) => T[]`; we instead default unconstrained T_map → unknown. Surface symptom: display.rs renders Array(UNKNOWN) as 'string[]' (Machine 1 territory). Fixing requires both: (a) higher-order generic inference in solver inference engine; (b) display correction in display.rs. Single-agent invariant fix is impossible. Forwarded to type-display-parity (display) and a future inference-engine campaign."
  ],
  "cross_cutting_blockers": [
    "2026-04-17: Many big3 'wrong-code' tests are NOT relation-kernel bugs but missing inference features. Specifically: (a) intra-expression inferences (TS 4.7) — needed by intraExpressionInferences.ts, intraExpressionInferencesJsx.tsx, and likely 5-10 more with shape '{producer: () => T, consumer: (T) => void}'. (b) generic function inference / higher-order generic inference (TS 3.4) — needed by genericFunctionInference1.ts (the pipe/flip examples on lines 27, 28, 188+). These are large feature implementations in the inference engine, not single-invariant fixes. Tier 2 agents should NOT attempt these — they belong to a dedicated 'inference-engine' campaign or staged by the integrator.",
    "2026-04-17 (CAMPAIGN EXHAUSTION ASSESSMENT): Across cycles 1–3 of /loop on big3 today (and 6 prior +0 sessions), every wrong-code candidate examined turned out to be a feature gap or other-campaign work, not a relation-kernel invariant. Inventory: (1) excessPropertyCheckIntersectionWithRecursiveType — recursive conditional type evaluation; (2) intraExpressionInferences[Jsx] — TS 4.7 feature; (3) genericFunctionInference1 — TS 3.4 + display; (4) coAndContraVariantInferences2/3/4 — lib.dom error tracking + variance inference; (5) inexistentPropertyInsideToStringType — JS function-this analysis; (6) checkOtherObjectAssignProperty — JS Object.defineProperty CommonJS; (7) jsdocPrototypePropertyAccessWithType — JSDoc @type on prototype; (8) contextualOverloadListFromUnionWithPrimitiveNoImplicitAny — contextual typing through union (different campaign); (9) mapUpsert — esnext lib loading; (10) await_incorrectThisType — awaits-non-thenable diagnostic, max +1 test impact; (11) globalThisAmbientModules — TS2536→TS2339 suppression-priority, max +1 test impact; (12) builtinIterator — overload resolution; (13) classCanExtendConstructorFunction — JS class extends; (14) checkJsxChildrenProperty4 — JSX. RECOMMENDATION TO INTEGRATOR: deallocate Tier 2 agents from big3-unification. Best remaining single-invariant fix in the wrong-code bucket is TS2536→TS2339 suppression in indexed_access.rs at +1 test, which fails Tier 2's >=3-tests-per-commit floor. Reallocate to: (a) Tier 1 fingerprint campaigns (498 tests at 50% impact ratio), (b) JS/JSDoc/CheckJs feature campaign covering #5–#7,#13, (c) inference-engine campaign for #1–#4. Big3 will close itself as those land."
  ]
}
