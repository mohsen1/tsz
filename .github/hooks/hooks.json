{
  "version": 1,
  "hooks": {
    "sessionEnd": [
      {
        "type": "command",
        "bash": "if [ -f session.sh ]; then ./session.sh; else echo 'Continue with work. You can always find new work to do if you have nothing to do. scripts/conformance.sh should be a good start. unit tests too.' >&2; exit 2; fi",
        "cwd": ".",
        "timeoutSec": 30
      }
    ]
  }
}
