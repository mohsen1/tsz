name: bench-image

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: bench-image-disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain GitHub Actions-only benchmark policy
        run: |
          echo "Benchmark base images are disabled."
          echo "TSZ benchmark builds now run only on GitHub Actions."
